<!-- ============================================ -->
<!--                 Side By Side                 -->
<!-- ============================================ -->

<section id="sbs-1123">
    <div class="cs-container">
        <!-- Left Image Section -->
        <div class="cs-image-group">
            <!--To make your own PNG cropped people, use https://www.remove.bg/ to remove the background of any image-->
            <!--Main Image-->
            <picture class="cs-picture cs-picture1">
                <source media="(max-width: 600px)" srcset="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/People/cropped-person.png">
                <source media="(min-width: 601px)" srcset="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/People/cropped-person.png">
                <img loading="lazy" decoding="async" src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/People/cropped-person.png" alt="person with box" width="519" height="782"  aria-hidden="true">
            </picture>
            <!--Image Mask-->
            <picture class="cs-picture cs-picture2">
                <source media="(max-width: 600px)" srcset="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/People/cropped-person.png">
                <source media="(min-width: 601px)" srcset="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/People/cropped-person.png">
                <img loading="lazy" decoding="async" src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/People/cropped-person.png" alt="person with box" width="519" height="782" aria-hidden="true">
            </picture>
            <!--Small Image-->
            <div class="cs-stat">
                <span class="cs-number">280K</span>
                <span class="cs-desc">People we have impacted with our work</span>
                <!--Green Blob-->
                <img class="cs-blob" loading="lazy" decoding="async" src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Icons/green-blob.svg" alt="graphic" width="195" height="204" aria-hidden="true">
                <!--Arrow-->
                <img class="cs-arrow" loading="lazy" decoding="async" src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Icons/squiggle-arrow2.svg" alt="graphic" width="130" height="130" aria-hidden="true">
            </div>
            <!--Circle Graphic-->
            <img class="cs-graphic" loading="lazy" decoding="async" src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Icons/circle-shape.svg" alt="graphic" width="662" height="679" aria-hidden="true">
        </div>
        <!-- Right Content Section-->
        <div class="cs-content">
            <span class="cs-topper">Stitch Non-profit Charity</span>
            <h2 class="cs-title">Helping Each Other Can Make World Better</h2>
            <p class="cs-text">
                We help companies develop powerful corporate social responsibility, grantmaking, and employee engagement strategies. Our impact is about more than moving money to where it’s needed most it’s also about helping nonprofits access information and ideas that will help them listen, learn, and grow.
            </p>
            <ul class="cs-ul">
                <li class="cs-li">Praesent feugiat sem mattis.</li>
                <li class="cs-li">Praesent feugiat sem mattis.</li>
                <li class="cs-li">Praesent feugiat sem mattis.</li>
                <li class="cs-li">Praesent feugiat sem mattis.</li>
            </ul>
            <a class="cs-button-solid" aria-label="learn more about our programs" href="">Discover More</a>
        </div>
    </div>
    <!--Charity hands graphic-->
    <img class="cs-floater" loading="lazy" decoding="async" src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Icons/charity-hands.svg" alt="hands" width="522" height="218" aria-hidden="true">
</section>
                                
/*-- -------------------------- -->
<---       Side By Side         -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
  :root {
    /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
    --primary: #00715D;
    --primaryLight: #d8673d;
    --secondary: #FDD65B;
    --secondaryLight: #FFBA43;
    --headerColor: #1a1a1a;
    --bodyTextColor: #4E4B66;
    --bodyTextColorWhite: #FAFBFC;
    /* 13px - 16px */
    --topperFontSize: clamp(0.8125rem, 1.6vw, 1rem);
    /* 31px - 49px */
    --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
    --bodyFontSize: 1rem;
    /* 60px - 100px top and bottom */
    --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
  }
  #sbs-1123 {
    /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
    font-family: 'Roboto', 'Arial', sans-serif;
    padding: var(--sectionPadding);
    background-color: #F7F7F7;
    position: relative;
    z-index: 1;
  }
  #sbs-1123 .cs-container {
    width: 100%;
    /* changes to 1280px at desktop */
    max-width: 34.375rem;
    margin: auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 3em;
  }
  #sbs-1123 .cs-content {
    /* set text align to left if content needs to be left aligned */
    text-align: left;
    width: 100%;
    max-width: 32.625rem;
    display: flex;
    flex-direction: column;
    /* centers content horizontally, set to flex-start to left align */
    align-items: flex-start;
  }
  #sbs-1123 .cs-topper {
    font-size: var(--topperFontSize);
    line-height: 1.2em;
    text-transform: uppercase;
    text-align: inherit;
    letter-spacing: .1em;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 0.25rem;
    display: block;
  }
  #sbs-1123 .cs-title {
    font-size: var(--headerFontSize);
    font-weight: 900;
    line-height: 1.2em;
    text-align: inherit;
    max-width: 43.75rem;
    margin: 0 0 1rem 0;
    color: var(--headerColor);
    position: relative;
  }
  #sbs-1123 .cs-text {
    font-size: var(--bodyFontSize);
    line-height: 1.5em;
    text-align: inherit;
    width: 100%;
    max-width: 40.625rem;
    margin: 0;
    color: var(--bodyTextColor);
  }
  #sbs-1123 .cs-topper {
    /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
    color: #767676;
  }
  #sbs-1123 .cs-text {
    /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
    margin-bottom: 1rem;
  }
  #sbs-1123 .cs-text:last-of-type {
    margin-bottom: 2rem;
  }
  #sbs-1123 .cs-ul {
    width: 100%;
    margin: 0 0 2rem 0;
    padding-left: 1.25rem;
    /* prevents padding from affecting width and height */
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.5rem;
  }
  #sbs-1123 .cs-li {
    font-size: var(--bodyFontSize);
    line-height: 1.5em;
    list-style: none;
    text-align: left;
    width: 100%;
    max-width: 25rem;
    margin: 0;
    color: var(--bodyTextColor);
    position: relative;
  }
  #sbs-1123 .cs-li:before {
    /* bullet */
    content: '';
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background: currentColor;
    opacity: 1;
    position: absolute;
    display: block;
    top: 0.625rem;
    left: -0.9375rem;
  }
  #sbs-1123 .cs-button-solid {
    font-size: 1rem;
    /* 46px - 56px */
    line-height: clamp(2.875rem, 5.5vw, 3.5rem);
    text-decoration: none;
    font-weight: 700;
    text-align: center;
    margin: 0;
    color: #fff;
    min-width: 12.5rem;
    padding: 0 1.5rem;
    background-color: var(--primary);
    border-radius: 0.5rem;
    display: inline-block;
    position: relative;
    z-index: 1;
    /* prevents padding from adding to the width */
    box-sizing: border-box;
  }
  #sbs-1123 .cs-button-solid:before {
    content: '';
    position: absolute;
    height: 100%;
    width: 0%;
    background: #000;
    opacity: 1;
    top: 0;
    left: 0;
    z-index: -1;
    border-radius: 0.5rem;
    transition: width .3s;
  }
  #sbs-1123 .cs-button-solid:hover:before {
    width: 100%;
  }
  #sbs-1123 .cs-image-group {
    /* scaling entire section down. font-size starts at a min in vw, and stops when that value reaches 1em (16px). Since we want the picture elements to base their font size on the parent and not the root, we use ems for this entire section  */
    font-size: min(2.18vw, .791em);
    width: 41.375em;
    height: 47.25em;
    position: relative;
    z-index: 1;
  }
  #sbs-1123 .cs-picture {
    /* width 100% so we can match the border radius with the circle graphic easier */
    width: 100%;
    height: 48.875em;
    display: flex;
    justify-content: center;
    align-items: flex-end;
    /* clips images to the shape of the parent */
    overflow: hidden;
    position: absolute;
    bottom: 0;
    left: 50%;
    z-index: 10;
    transform: translateX(-50%);
  }
  #sbs-1123 .cs-picture img {
    width: auto;
    height: 48.875em;
    position: relative;
    bottom: -1.125em;
    left: 0;
  }
  #sbs-1123 .cs-picture1 {
    /* this is set to match the curve of the circle graphic behind it */
    border-radius: 0 0 47.8% 47.4%;
  }
  #sbs-1123 .cs-picture2 {
    /* this is the picture mask, we make the picture element a specific height and width thats smaller than the actual image, and where the actual image overflows the new dimensions it gets cut off by the overflow:hidden.  This crop is the image that overlaps the circle shapes we created.  */
    width: 25.8125em;
    height: 46.4375em;
    border-radius: 0 0 1.1875em 0;
    /* place the cropped image exactly over the cs-picture1 to finish the masking effect */
    left: 17.3125em;
    bottom: 1.3125em;
  }
  #sbs-1123 .cs-picture2 img {
    position: absolute;
    top: 0;
    left: 0;
  }
  #sbs-1123 .cs-graphic {
    width: 100%;
    height: auto;
    position: absolute;
    bottom: 0;
    left: 0;
    z-index: -1;
  }
  #sbs-1123 .cs-stat {
    text-align: center;
    display: inline-flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    gap: 0.25em;
    position: absolute;
    top: 4.625em;
    left: 1.75em;
    z-index: 1;
  }
  #sbs-1123 .cs-number {
    /* 20px - 39px */
    font-size: clamp(1.25rem, 3vw, 2.4375rem);
    line-height: 1.2em;
    font-weight: 700;
    text-align: inherit;
    margin: 0;
    color: var(--bodyTextColorWhite);
  }
  #sbs-1123 .cs-desc {
    /* 10px - 14px */
    font-size: clamp(0.625rem, 1vw, 0.875rem);
    line-height: 1.5em;
    text-align: inherit;
    width: 100%;
    /* 80px - 148px */
    max-width: clamp(5rem, 11vw, 9.25rem);
    margin: 0;
    color: var(--bodyTextColorWhite);
  }
  #sbs-1123 .cs-blob {
    width: 142%;
    max-width: 12.5rem;
    height: auto;
    position: absolute;
    top: 50%;
    left: 50%;
    z-index: -1;
    transform: translate(-50%, -50%);
  }
  #sbs-1123 .cs-arrow {
    width: 8.125em;
    height: auto;
    position: absolute;
    top: clamp(150%, 14vw, 180%);
    left: 0.3125em;
    transform: rotate(-52deg);
  }
  #sbs-1123 .cs-floater {
    /* 270px - 522px */
    width: clamp(16.875rem, 40vw, 32.625rem);
    height: auto;
    position: absolute;
    right: 0;
    bottom: 0;
    z-index: -1;
  }
}
/* Desktop - 1024px */
@media only screen and (min-width: 64rem) {
  #sbs-1123 .cs-container {
    max-width: 80rem;
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
  }
  #sbs-1123 .cs-image-group {
    /* reset the scale */
    font-size: min(1.2vw, 1em);
    /* prevent flexbox from squishing it */
    flex: none;
  }
  #sbs-1123 .cs-content {
    width: 50%;
  }
}
                                
/*-- -------------------------- -->
<---       Side By Side         -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
  :root {
    /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
    --primary: #00715D;
    --primaryLight: #d8673d;
    --secondary: #FDD65B;
    --secondaryLight: #FFBA43;
    --headerColor: #1a1a1a;
    --bodyTextColor: #4E4B66;
    --bodyTextColorWhite: #FAFBFC;
    /* 13px - 16px */
    --topperFontSize: clamp(0.8125rem, 1.6vw, 1rem);
    /* 31px - 49px */
    --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
    --bodyFontSize: 1rem;
    /* 60px - 100px top and bottom */
    --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
  }
  #sbs-1123 {
    /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
    font-family: 'Roboto', 'Arial', sans-serif;
    padding: var(--sectionPadding);
    background-color: #F7F7F7;
    position: relative;
    z-index: 1;
  }
  #sbs-1123 .cs-container {
    width: 100%;
    /* changes to 1280px at desktop */
    max-width: 34.375rem;
    margin: auto;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 3em;
  }
  #sbs-1123 .cs-content {
    /* set text align to left if content needs to be left aligned */
    text-align: left;
    width: 100%;
    max-width: 32.625rem;
    display: flex;
    flex-direction: column;
    /* centers content horizontally, set to flex-start to left align */
    align-items: flex-start;
  }
  #sbs-1123 .cs-topper {
    font-size: var(--topperFontSize);
    line-height: 1.2em;
    text-transform: uppercase;
    text-align: inherit;
    letter-spacing: .1em;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 0.25rem;
    display: block;
  }
  #sbs-1123 .cs-title {
    font-size: var(--headerFontSize);
    font-weight: 900;
    line-height: 1.2em;
    text-align: inherit;
    max-width: 43.75rem;
    margin: 0 0 1rem 0;
    color: var(--headerColor);
    position: relative;
  }
  #sbs-1123 .cs-text {
    font-size: var(--bodyFontSize);
    line-height: 1.5em;
    text-align: inherit;
    width: 100%;
    max-width: 40.625rem;
    margin: 0;
    color: var(--bodyTextColor);
  }
  #sbs-1123 .cs-topper {
    /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
    color: #767676;
  }
  #sbs-1123 .cs-text {
    /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
    margin-bottom: 1rem;
  }
  #sbs-1123 .cs-text:last-of-type {
    margin-bottom: 2rem;
  }
  #sbs-1123 .cs-ul {
    width: 100%;
    margin: 0 0 2rem 0;
    padding-left: 1.25rem;
    /* prevents padding from affecting width and height */
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.5rem;
  }
  #sbs-1123 .cs-li {
    font-size: var(--bodyFontSize);
    line-height: 1.5em;
    list-style: none;
    text-align: left;
    width: 100%;
    max-width: 25rem;
    margin: 0;
    color: var(--bodyTextColor);
    position: relative;
  }
  #sbs-1123 .cs-li:before {
    /* bullet */
    content: '';
    width: 4px;
    height: 4px;
    border-radius: 50%;
    background: currentColor;
    opacity: 1;
    position: absolute;
    display: block;
    top: 0.625rem;
    left: -0.9375rem;
  }
  #sbs-1123 .cs-button-solid {
    font-size: 1rem;
    /* 46px - 56px */
    line-height: clamp(2.875rem, 5.5vw, 3.5rem);
    text-decoration: none;
    font-weight: 700;
    text-align: center;
    margin: 0;
    color: #fff;
    min-width: 12.5rem;
    padding: 0 1.5rem;
    background-color: var(--primary);
    border-radius: 0.5rem;
    display: inline-block;
    position: relative;
    z-index: 1;
    /* prevents padding from adding to the width */
    box-sizing: border-box;
  }
  #sbs-1123 .cs-button-solid:before {
    content: '';
    position: absolute;
    height: 100%;
    width: 0%;
    background: #000;
    opacity: 1;
    top: 0;
    left: 0;
    z-index: -1;
    border-radius: 0.5rem;
    transition: width .3s;
  }
  #sbs-1123 .cs-button-solid:hover:before {
    width: 100%;
  }
  #sbs-1123 .cs-image-group {
    /* scaling entire section down. font-size starts at a min in vw, and stops when that value reaches 1em (16px). Since we want the picture elements to base their font size on the parent and not the root, we use ems for this entire section  */
    font-size: min(2.18vw, .791em);
    width: 41.375em;
    height: 47.25em;
    position: relative;
    z-index: 1;
  }
  #sbs-1123 .cs-picture {
    /* width 100% so we can match the border radius with the circle graphic easier */
    width: 100%;
    height: 48.875em;
    display: flex;
    justify-content: center;
    align-items: flex-end;
    /* clips images to the shape of the parent */
    overflow: hidden;
    position: absolute;
    bottom: 0;
    left: 50%;
    z-index: 10;
    transform: translateX(-50%);
  }
  #sbs-1123 .cs-picture img {
    width: auto;
    height: 48.875em;
    position: relative;
    bottom: -1.125em;
    left: 0;
  }
  #sbs-1123 .cs-picture1 {
    /* this is set to match the curve of the circle graphic behind it */
    border-radius: 0 0 47.8% 47.4%;
  }
  #sbs-1123 .cs-picture2 {
    /* this is the picture mask, we make the picture element a specific height and width thats smaller than the actual image, and where the actual image overflows the new dimensions it gets cut off by the overflow:hidden.  This crop is the image that overlaps the circle shapes we created.  */
    width: 25.8125em;
    height: 46.4375em;
    border-radius: 0 0 1.1875em 0;
    /* place the cropped image exactly over the cs-picture1 to finish the masking effect */
    left: 17.3125em;
    bottom: 1.3125em;
  }
  #sbs-1123 .cs-picture2 img {
    position: absolute;
    top: 0;
    left: 0;
  }
  #sbs-1123 .cs-graphic {
    width: 100%;
    height: auto;
    position: absolute;
    bottom: 0;
    left: 0;
    z-index: -1;
  }
  #sbs-1123 .cs-stat {
    text-align: center;
    display: inline-flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    gap: 0.25em;
    position: absolute;
    top: 4.625em;
    left: 1.75em;
    z-index: 1;
  }
  #sbs-1123 .cs-number {
    /* 20px - 39px */
    font-size: clamp(1.25rem, 3vw, 2.4375rem);
    line-height: 1.2em;
    font-weight: 700;
    text-align: inherit;
    margin: 0;
    color: var(--bodyTextColorWhite);
  }
  #sbs-1123 .cs-desc {
    /* 10px - 14px */
    font-size: clamp(0.625rem, 1vw, 0.875rem);
    line-height: 1.5em;
    text-align: inherit;
    width: 100%;
    /* 80px - 148px */
    max-width: clamp(5rem, 11vw, 9.25rem);
    margin: 0;
    color: var(--bodyTextColorWhite);
  }
  #sbs-1123 .cs-blob {
    width: 142%;
    max-width: 12.5rem;
    height: auto;
    position: absolute;
    top: 50%;
    left: 50%;
    z-index: -1;
    transform: translate(-50%, -50%);
  }
  #sbs-1123 .cs-arrow {
    width: 8.125em;
    height: auto;
    position: absolute;
    top: clamp(150%, 14vw, 180%);
    left: 0.3125em;
    transform: rotate(-52deg);
  }
  #sbs-1123 .cs-floater {
    /* 270px - 522px */
    width: clamp(16.875rem, 40vw, 32.625rem);
    height: auto;
    position: absolute;
    right: 0;
    bottom: 0;
    z-index: -1;
  }
}
/* Desktop - 1024px */
@media only screen and (min-width: 64rem) {
  #sbs-1123 .cs-container {
    max-width: 80rem;
    flex-direction: row;
    align-items: center;
    justify-content: space-between;
  }
  #sbs-1123 .cs-image-group {
    /* reset the scale */
    font-size: min(1.2vw, 1em);
    /* prevent flexbox from squishing it */
    flex: none;
  }
  #sbs-1123 .cs-content {
    width: 50%;
  }
}
/* Dark Mode */
@media only screen and (min-width: 0rem) {
  body.dark-mode #sbs-1123 {
    background-color: rgba(0, 0, 0, 0.2);
  }
  body.dark-mode #sbs-1123 .cs-topper {
    color: var(--secondary);
  }
  body.dark-mode #sbs-1123 .cs-title,
  body.dark-mode #sbs-1123 .cs-text,
  body.dark-mode #sbs-1123 .cs-li {
    color: var(--bodyTextColorWhite);
  }
  body.dark-mode #sbs-1123 .cs-floater {
    opacity: .2;
  }
}
                                
/*-- -------------------------- -->
<---       Side By Side         -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
    :root {
        /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
        --primary: #00715D;
        --primaryLight: #d8673d;
        --secondary: #FDD65B;
        --secondaryLight: #FFBA43;
        --headerColor: #1a1a1a;
        --bodyTextColor: #4E4B66;
		--bodyTextColorWhite: #FAFBFC;

        /* 13px - 16px */
        --topperFontSize: clamp(.8125rem, 1.6vw, 1rem);
        /* 31px - 49px */
        --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
        --bodyFontSize: 1rem;

        /* 60px - 100px top and bottom */
        --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
    }

    #sbs-1123 {
        /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
        font-family: 'Roboto', 'Arial', sans-serif;
        padding: var(--sectionPadding);
        background-color: #F7F7F7;
        position: relative;
        z-index: 1;

        .cs-container {
            width: 100%;
            /* changes to 1280px at desktop */
            max-width: (550/16rem);
            margin: auto;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: (48/16em);
        }

        .cs-content {
            /* set text align to left if content needs to be left aligned */
            text-align: left;
            width: 100%;
            max-width: (522/16rem);
            display: flex;
            flex-direction: column;
            /* centers content horizontally, set to flex-start to left align */
            align-items: flex-start;
        }
    
        .cs-topper {
            font-size: var(--topperFontSize);
            line-height: 1.2em;
            text-transform: uppercase;
            text-align: inherit;
            letter-spacing: .1em;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: (4/16rem);
            display: block;
        }
    
        .cs-title {
            font-size: var(--headerFontSize);
            font-weight: 900;
            line-height: 1.2em;
            text-align: inherit;
            max-width: (700/16rem);
            margin: 0 0 (16/16rem) 0;
            color: var(--headerColor);
            position: relative;
        }
    
        .cs-text {
            font-size: var(--bodyFontSize);
            line-height: 1.5em;
            text-align: inherit;
            width: 100%;
            max-width: (650/16rem);
            margin: 0;
            color: var(--bodyTextColor);
        }

        .cs-topper {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            color: #767676;
        }

        .cs-text {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            margin-bottom: (16/16rem);
            &:last-of-type {
                margin-bottom: (32/16rem);
            }
        }

        .cs-ul {
            width: 100%;
            margin: 0 0 (32/16rem) 0;
            padding-left: (20/16rem);
            /* prevents padding from affecting width and height */
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            gap: (8/16rem);
        }

        .cs-li {
            font-size: var(--bodyFontSize);
            line-height: 1.5em;
            list-style: none;
            text-align: left;
            width: 100%;
            max-width: (400/16rem);
            margin: 0;
            color: var(--bodyTextColor);
            position: relative;

            &:before {
                /* bullet */
                content: '';
                width: 4px;
                height: 4px;
                border-radius: 50%;
                background: currentColor;
                opacity: 1;
                position: absolute;
                display: block;
                top: (10/16rem);
                left: (-15/16rem);
            }
        }

        .cs-button-solid {
            font-size: (16/16rem);
            /* 46px - 56px */
            line-height: clamp(2.875rem, 5.5vw, 3.5rem);
            text-decoration: none;
	    	font-weight: 700;
	        text-align: center;
            margin: 0;
            color: #fff;
            min-width: (200/16rem);
            padding: 0 (24/16rem);
            background-color: var(--primary);
            border-radius: (8/16rem);
            display: inline-block;
            position: relative;
            z-index: 1;
            /* prevents padding from adding to the width */
            box-sizing: border-box;

            &:before {
                content: '';
                position: absolute;
                height: 100%;
                width: 0%;
                background: #000;
                opacity: 1;
                top: 0;
                left: 0;
                z-index: -1;
                border-radius: (8/16rem);
                transition: width .3s;
            }

            &:hover {
                &:before {
                    width: 100%;
                }
            }
        }

        .cs-image-group {
            /* scaling entire section down. font-size starts at a min in vw, and stops when that value reaches 1em (16px). Since we want the picture elements to base their font size on the parent and not the root, we use ems for this entire section  */
            font-size:~"min(2.18vw, .791em)";
            width: (662/16em);
            height: (756/16em);
            position: relative;
            z-index: 1;
        }

        .cs-picture {
            /* width 100% so we can match the border radius with the circle graphic easier */
            width: 100%;
            height: (782/16em);
            display: flex;
            justify-content: center;
            align-items: flex-end;
            /* clips images to the shape of the parent */
            overflow: hidden;
            position: absolute;
            bottom: 0;
            left: 50%;
            z-index: 10;
            transform: translateX(-50%);

            img {
                width: auto;
                height: (782/16em);
                position: relative;
                bottom: (-18/16em);
                left: 0;
            }
        }

        .cs-picture1 {
            /* this is set to match the curve of the circle graphic behind it */
            border-radius: 0 0 47.8% 47.4%;
        }

        .cs-picture2 {
            /* this is the picture mask, we make the picture element a specific height and width thats smaller than the actual image, and where the actual image overflows the new dimensions it gets cut off by the overflow:hidden.  This crop is the image that overlaps the circle shapes we created.  */
            width: (413/16em);
            height: (743/16em);
            border-radius: 0 0 (19/16em) 0;
            /* place the cropped image exactly over the cs-picture1 to finish the masking effect */
            left: (277/16em);
            bottom: (21/16em);
            

            img {
                position: absolute;
                top: 0;
                left: 0;
            }
        }

        .cs-graphic {
            width: 100%;
            height: auto;
            position: absolute;
            bottom: 0;
            left: 0;
            z-index: -1;
        }

        .cs-stat {
            text-align: center;
            display: inline-flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            gap: (4/16em);
            position: absolute;
            top: (74/16em);
            left: (28/16em);
            z-index: 1;
        }

        .cs-number {
            /* 20px - 39px */
            font-size: clamp(1.25rem, 3vw, 2.4375rem);
            line-height: 1.2em;
            font-weight: 700;
            text-align: inherit;
            margin: 0;
            color: var(--bodyTextColorWhite);
        }

        .cs-desc {
            /* 10px - 14px */
            font-size: clamp(.625rem, 1vw, .875rem);
            line-height: 1.5em;
            text-align: inherit;
            width: 100%;
            /* 80px - 148px */
            max-width: clamp(5rem, 11vw, 9.25rem);
            margin: 0;
            color: var(--bodyTextColorWhite);
        }

        .cs-blob {
            width: 142%;
            max-width: (200/16rem);
            height: auto;
            position: absolute;
            top: 50%;
            left: 50%;
            z-index: -1;
            transform: translate(-50%, -50%);
        }

        .cs-arrow {
            width: (130/16em);
            height: auto;
            position: absolute;
            top: clamp(150%, 14vw, 180%);
            left: (5/16em);
            transform: rotate(-52deg);
        }

        .cs-floater {
            /* 270px - 522px */
            width: clamp(16.875rem, 40vw, 32.625rem);
            height: auto;
            position: absolute;
            right: 0;
            bottom: 0;
            z-index: -1;
        }
    }
}

/* Desktop - 1024px */
@media only screen and (min-width: 64rem) {
    #sbs-1123 {
        .cs-container {
            max-width: (1280/16rem);
            flex-direction: row;
            align-items: center;
            justify-content: space-between;
        }

        .cs-image-group {
            /* reset the scale */
            font-size:~"min(1.2vw, 1em)";
            /* prevent flexbox from squishing it */
            flex: none;
        }

        .cs-content {
            width: 50%;
        }
    }
}
                                
/*-- -------------------------- -->
<---       Side By Side         -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
    :root {
        /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
        --primary: #00715D;
        --primaryLight: #d8673d;
        --secondary: #FDD65B;
        --secondaryLight: #FFBA43;
        --headerColor: #1a1a1a;
        --bodyTextColor: #4E4B66;
		--bodyTextColorWhite: #FAFBFC;

        /* 13px - 16px */
        --topperFontSize: clamp(.8125rem, 1.6vw, 1rem);
        /* 31px - 49px */
        --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
        --bodyFontSize: 1rem;

        /* 60px - 100px top and bottom */
        --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
    }

    #sbs-1123 {
        /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
        font-family: 'Roboto', 'Arial', sans-serif;
        padding: var(--sectionPadding);
        background-color: #F7F7F7;
        position: relative;
        z-index: 1;

        .cs-container {
            width: 100%;
            /* changes to 1280px at desktop */
            max-width: (550/16rem);
            margin: auto;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: (48/16em);
        }

        .cs-content {
            /* set text align to left if content needs to be left aligned */
            text-align: left;
            width: 100%;
            max-width: (522/16rem);
            display: flex;
            flex-direction: column;
            /* centers content horizontally, set to flex-start to left align */
            align-items: flex-start;
        }
    
        .cs-topper {
            font-size: var(--topperFontSize);
            line-height: 1.2em;
            text-transform: uppercase;
            text-align: inherit;
            letter-spacing: .1em;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: (4/16rem);
            display: block;
        }
    
        .cs-title {
            font-size: var(--headerFontSize);
            font-weight: 900;
            line-height: 1.2em;
            text-align: inherit;
            max-width: (700/16rem);
            margin: 0 0 (16/16rem) 0;
            color: var(--headerColor);
            position: relative;
        }
    
        .cs-text {
            font-size: var(--bodyFontSize);
            line-height: 1.5em;
            text-align: inherit;
            width: 100%;
            max-width: (650/16rem);
            margin: 0;
            color: var(--bodyTextColor);
        }

        .cs-topper {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            color: #767676;
        }

        .cs-text {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            margin-bottom: (16/16rem);
            &:last-of-type {
                margin-bottom: (32/16rem);
            }
        }

        .cs-ul {
            width: 100%;
            margin: 0 0 (32/16rem) 0;
            padding-left: (20/16rem);
            /* prevents padding from affecting width and height */
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            gap: (8/16rem);
        }

        .cs-li {
            font-size: var(--bodyFontSize);
            line-height: 1.5em;
            list-style: none;
            text-align: left;
            width: 100%;
            max-width: (400/16rem);
            margin: 0;
            color: var(--bodyTextColor);
            position: relative;

            &:before {
                /* bullet */
                content: '';
                width: 4px;
                height: 4px;
                border-radius: 50%;
                background: currentColor;
                opacity: 1;
                position: absolute;
                display: block;
                top: (10/16rem);
                left: (-15/16rem);
            }
        }

        .cs-button-solid {
            font-size: (16/16rem);
            /* 46px - 56px */
            line-height: clamp(2.875rem, 5.5vw, 3.5rem);
            text-decoration: none;
	    	font-weight: 700;
	        text-align: center;
            margin: 0;
            color: #fff;
            min-width: (200/16rem);
            padding: 0 (24/16rem);
            background-color: var(--primary);
            border-radius: (8/16rem);
            display: inline-block;
            position: relative;
            z-index: 1;
            /* prevents padding from adding to the width */
            box-sizing: border-box;

            &:before {
                content: '';
                position: absolute;
                height: 100%;
                width: 0%;
                background: #000;
                opacity: 1;
                top: 0;
                left: 0;
                z-index: -1;
                border-radius: (8/16rem);
                transition: width .3s;
            }

            &:hover {
                &:before {
                    width: 100%;
                }
            }
        }

        .cs-image-group {
            /* scaling entire section down. font-size starts at a min in vw, and stops when that value reaches 1em (16px). Since we want the picture elements to base their font size on the parent and not the root, we use ems for this entire section  */
            font-size:~"min(2.18vw, .791em)";
            width: (662/16em);
            height: (756/16em);
            position: relative;
            z-index: 1;
        }

        .cs-picture {
            /* width 100% so we can match the border radius with the circle graphic easier */
            width: 100%;
            height: (782/16em);
            display: flex;
            justify-content: center;
            align-items: flex-end;
            /* clips images to the shape of the parent */
            overflow: hidden;
            position: absolute;
            bottom: 0;
            left: 50%;
            z-index: 10;
            transform: translateX(-50%);

            img {
                width: auto;
                height: (782/16em);
                position: relative;
                bottom: (-18/16em);
                left: 0;
            }
        }

        .cs-picture1 {
            /* this is set to match the curve of the circle graphic behind it */
            border-radius: 0 0 47.8% 47.4%;
        }

        .cs-picture2 {
            /* this is the picture mask, we make the picture element a specific height and width thats smaller than the actual image, and where the actual image overflows the new dimensions it gets cut off by the overflow:hidden.  This crop is the image that overlaps the circle shapes we created.  */
            width: (413/16em);
            height: (743/16em);
            border-radius: 0 0 (19/16em) 0;
            /* place the cropped image exactly over the cs-picture1 to finish the masking effect */
            left: (277/16em);
            bottom: (21/16em);
            

            img {
                position: absolute;
                top: 0;
                left: 0;
            }
        }

        .cs-graphic {
            width: 100%;
            height: auto;
            position: absolute;
            bottom: 0;
            left: 0;
            z-index: -1;
        }

        .cs-stat {
            text-align: center;
            display: inline-flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            gap: (4/16em);
            position: absolute;
            top: (74/16em);
            left: (28/16em);
            z-index: 1;
        }

        .cs-number {
            /* 20px - 39px */
            font-size: clamp(1.25rem, 3vw, 2.4375rem);
            line-height: 1.2em;
            font-weight: 700;
            text-align: inherit;
            margin: 0;
            color: var(--bodyTextColorWhite);
        }

        .cs-desc {
            /* 10px - 14px */
            font-size: clamp(.625rem, 1vw, .875rem);
            line-height: 1.5em;
            text-align: inherit;
            width: 100%;
            /* 80px - 148px */
            max-width: clamp(5rem, 11vw, 9.25rem);
            margin: 0;
            color: var(--bodyTextColorWhite);
        }

        .cs-blob {
            width: 142%;
            max-width: (200/16rem);
            height: auto;
            position: absolute;
            top: 50%;
            left: 50%;
            z-index: -1;
            transform: translate(-50%, -50%);
        }

        .cs-arrow {
            width: (130/16em);
            height: auto;
            position: absolute;
            top: clamp(150%, 14vw, 180%);
            left: (5/16em);
            transform: rotate(-52deg);
        }

        .cs-floater {
            /* 270px - 522px */
            width: clamp(16.875rem, 40vw, 32.625rem);
            height: auto;
            position: absolute;
            right: 0;
            bottom: 0;
            z-index: -1;
        }
    }
}

/* Desktop - 1024px */
@media only screen and (min-width: 64rem) {
    #sbs-1123 {
        .cs-container {
            max-width: (1280/16rem);
            flex-direction: row;
            align-items: center;
            justify-content: space-between;
        }

        .cs-image-group {
            /* reset the scale */
            font-size:~"min(1.2vw, 1em)";
            /* prevent flexbox from squishing it */
            flex: none;
        }

        .cs-content {
            width: 50%;
        }
    }
}

/* Dark Mode */
@media only screen and (min-width: 0rem) {
    body.dark-mode {
        #sbs-1123 {
            background-color: rgba(0, 0, 0, .2);
            .cs-topper {
                color: var(--secondary);
            }

            .cs-title, .cs-text, .cs-li {
                color: var(--bodyTextColorWhite);
            }

            .cs-floater {
                opacity: .2;
            }
        }
    }
}
                                
/*-- -------------------------- -->
<---       Side By Side         -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
    :root {
        /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
        --primary: #00715D;
        --primaryLight: #d8673d;
        --secondary: #FDD65B;
        --secondaryLight: #FFBA43;
        --headerColor: #1a1a1a;
        --bodyTextColor: #4E4B66;
		--bodyTextColorWhite: #FAFBFC;

        /* 13px - 16px */
        --topperFontSize: clamp(.8125rem, 1.6vw, 1rem);
        /* 31px - 49px */
        --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
        --bodyFontSize: 1rem;

        /* 60px - 100px top and bottom */
        --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
    }

    #sbs-1123 {
        /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
        font-family: 'Roboto', 'Arial', sans-serif;
        padding: var(--sectionPadding);
        background-color: #F7F7F7;
        position: relative;
        z-index: 1;

        .cs-container {
            width: 100%;
            /* changes to 1280px at desktop */
            max-width: calc(550 / 16 * 1rem);
            margin: auto;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: calc(48 / 16 * 1rem);
        }

        .cs-content {
            /* set text align to left if content needs to be left aligned */
            text-align: left;
            width: 100%;
            max-width: calc(522 / 16 * 1rem);
            display: flex;
            flex-direction: column;
            /* centers content horizontally, set to flex-start to left align */
            align-items: flex-start;
        }
    
        .cs-topper {
            font-size: var(--topperFontSize);
            line-height: 1.2em;
            text-transform: uppercase;
            text-align: inherit;
            letter-spacing: .1em;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: calc(4 / 16 * 1rem);
            display: block;
        }
    
        .cs-title {
            font-size: var(--headerFontSize);
            font-weight: 900;
            line-height: 1.2em;
            text-align: inherit;
            max-width: calc(700 / 16 * 1rem);
            margin: 0 0 calc(16 / 16 * 1rem) 0;
            color: var(--headerColor);
            position: relative;
        }
    
        .cs-text {
            font-size: var(--bodyFontSize);
            line-height: 1.5em;
            text-align: inherit;
            width: 100%;
            max-width: calc(650 / 16 * 1rem);
            margin: 0;
            color: var(--bodyTextColor);
        }

        .cs-topper {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            color: #767676;
        }

        .cs-text {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            margin-bottom: calc(16 / 16 * 1rem);
            &:last-of-type {
                margin-bottom: calc(32 / 16 * 1rem);
            }
        }

        .cs-ul {
            width: 100%;
            margin: 0 0 calc(32 / 16 * 1rem) 0;
            padding-left: calc(20 / 16 * 1rem);
            /* prevents padding from affecting width and height */
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            gap: calc(8 / 16 * 1rem);
        }

        .cs-li {
            font-size: var(--bodyFontSize);
            line-height: 1.5em;
            list-style: none;
            text-align: left;
            width: 100%;
            max-width: calc(400 / 16 * 1rem);
            margin: 0;
            color: var(--bodyTextColor);
            position: relative;

            &:before {
                /* bullet */
                content: '';
                width: 4px;
                height: 4px;
                border-radius: 50%;
                background: currentColor;
                opacity: 1;
                position: absolute;
                display: block;
                top: calc(10 / 16 * 1rem);
                left: calc(-15 / 16 * 1rem);
            }
        }

        .cs-button-solid {
            font-size: calc(16 / 16 * 1rem);
            /* 46px - 56px */
            line-height: clamp(2.875rem, 5.5vw, 3.5rem);
            text-decoration: none;
	    	font-weight: 700;
	        text-align: center;
            margin: 0;
            color: #fff;
            min-width: calc(200 / 16 * 1rem);
            padding: 0 calc(24 / 16 * 1rem);
            background-color: var(--primary);
            border-radius: calc(8 / 16 * 1rem);
            display: inline-block;
            position: relative;
            z-index: 1;
            /* prevents padding from adding to the width */
            box-sizing: border-box;

            &:before {
                content: '';
                position: absolute;
                height: 100%;
                width: 0%;
                background: #000;
                opacity: 1;
                top: 0;
                left: 0;
                z-index: -1;
                border-radius: calc(8 / 16 * 1rem);
                transition: width .3s;
            }

            &:hover {
                &:before {
                    width: 100%;
                }
            }
        }

        .cs-image-group {
            /* scaling entire section down. font-size starts at a min in vw, and stops when that value reaches 1em (16px). Since we want the picture elements to base their font size on the parent and not the root, we use ems for this entire section  */
            font-size: min(2.18vw, .791em);
            width: calc(662 / 16 * 1em);
            height: calc(756 / 16 * 1em);
            position: relative;
            z-index: 1;
        }

        .cs-picture {
            /* width 100% so we can match the border radius with the circle graphic easier */
            width: 100%;
            height: calc(782 / 16 * 1em);
            display: flex;
            justify-content: center;
            align-items: flex-end;
            /* clips images to the shape of the parent */
            overflow: hidden;
            position: absolute;
            bottom: 0;
            left: 50%;
            z-index: 10;
            transform: translateX(-50%);

            img {
                width: auto;
                height: calc(782 / 16 * 1em);
                position: relative;
                bottom: calc(-18 / 16 * 1em);
                left: 0;
            }
        }

        .cs-picture1 {
            /* this is set to match the curve of the circle graphic behind it */
            border-radius: 0 0 47.8% 47.4%;
        }

        .cs-picture2 {
            /* this is the picture mask, we make the picture element a specific height and width thats smaller than the actual image, and where the actual image overflows the new dimensions it gets cut off by the overflow:hidden.  This crop is the image that overlaps the circle shapes we created.  */
            width: calc(413 / 16 * 1em);
            height: calc(743 / 16 * 1em);
            border-radius: 0 0 calc(19 / 16 * 1em) 0;
            /* place the cropped image exactly over the cs-picture1 to finish the masking effect */
            left: calc(277 / 16 * 1em);
            bottom: calc(21 / 16 * 1em);
            

            img {
                position: absolute;
                top: 0;
                left: 0;
            }
        }

        .cs-graphic {
            width: 100%;
            height: auto;
            position: absolute;
            bottom: 0;
            left: 0;
            z-index: -1;
        }

        .cs-stat {
            text-align: center;
            display: inline-flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            gap: calc(4 / 16 * 1rem);
            position: absolute;
            top: calc(74 / 16 * 1em);
            left: calc(28 / 16 * 1em);
            z-index: 1;
        }

        .cs-number {
            /* 20px - 39px */
            font-size: clamp(1.25rem, 3vw, 2.4375rem);
            line-height: 1.2em;
            font-weight: 700;
            text-align: inherit;
            margin: 0;
            color: var(--bodyTextColorWhite);
        }

        .cs-desc {
            /* 10px - 14px */
            font-size: clamp(.625rem, 1vw, .875rem);
            line-height: 1.5em;
            text-align: inherit;
            width: 100%;
            /* 80px - 148px */
            max-width: clamp(5rem, 11vw, 9.25rem);
            margin: 0;
            color: var(--bodyTextColorWhite);
        }

        .cs-blob {
            width: 142%;
            max-width: calc(200 / 16 * 1rem);
            height: auto;
            position: absolute;
            top: 50%;
            left: 50%;
            z-index: -1;
            transform: translate(-50%, -50%);
        }

        .cs-arrow {
            width: calc(130 / 16 * 1em);
            height: auto;
            position: absolute;
            top: clamp(150%, 14vw, 180%);
            left: calc(5 / 16 * 1em);
            transform: rotate(-52deg);
        }

        .cs-floater {
            /* 270px - 522px */
            width: clamp(16.875rem, 40vw, 32.625rem);
            height: auto;
            position: absolute;
            right: 0;
            bottom: 0;
            z-index: -1;
        }
    }
}

/* Desktop - 1024px */
@media only screen and (min-width: 64rem) {
    #sbs-1123 {
        .cs-container {
            max-width: calc(1280 / 16 * 1rem);
            flex-direction: row;
            align-items: center;
            justify-content: space-between;
        }

        .cs-image-group {
            /* reset the scale */
            font-size: min(1.2vw, 1em);
            /* prevent flexbox from squishing it */
            flex: none;
        }

        .cs-content {
            width: 50%;
        }
    }
}
                                
/*-- -------------------------- -->
<---       Side By Side         -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
    :root {
        /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
        --primary: #00715D;
        --primaryLight: #d8673d;
        --secondary: #FDD65B;
        --secondaryLight: #FFBA43;
        --headerColor: #1a1a1a;
        --bodyTextColor: #4E4B66;
		--bodyTextColorWhite: #FAFBFC;

        /* 13px - 16px */
        --topperFontSize: clamp(.8125rem, 1.6vw, 1rem);
        /* 31px - 49px */
        --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
        --bodyFontSize: 1rem;

        /* 60px - 100px top and bottom */
        --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
    }

    #sbs-1123 {
        /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
        font-family: 'Roboto', 'Arial', sans-serif;
        padding: var(--sectionPadding);
        background-color: #F7F7F7;
        position: relative;
        z-index: 1;

        .cs-container {
            width: 100%;
            /* changes to 1280px at desktop */
            max-width: calc(550 / 16 * 1rem);
            margin: auto;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: calc(48 / 16 * 1rem);
        }

        .cs-content {
            /* set text align to left if content needs to be left aligned */
            text-align: left;
            width: 100%;
            max-width: calc(522 / 16 * 1rem);
            display: flex;
            flex-direction: column;
            /* centers content horizontally, set to flex-start to left align */
            align-items: flex-start;
        }
    
        .cs-topper {
            font-size: var(--topperFontSize);
            line-height: 1.2em;
            text-transform: uppercase;
            text-align: inherit;
            letter-spacing: .1em;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: calc(4 / 16 * 1rem);
            display: block;
        }
    
        .cs-title {
            font-size: var(--headerFontSize);
            font-weight: 900;
            line-height: 1.2em;
            text-align: inherit;
            max-width: calc(700 / 16 * 1rem);
            margin: 0 0 calc(16 / 16 * 1rem) 0;
            color: var(--headerColor);
            position: relative;
        }
    
        .cs-text {
            font-size: var(--bodyFontSize);
            line-height: 1.5em;
            text-align: inherit;
            width: 100%;
            max-width: calc(650 / 16 * 1rem);
            margin: 0;
            color: var(--bodyTextColor);
        }

        .cs-topper {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            color: #767676;
        }

        .cs-text {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            margin-bottom: calc(16 / 16 * 1rem);
            &:last-of-type {
                margin-bottom: calc(32 / 16 * 1rem);
            }
        }

        .cs-ul {
            width: 100%;
            margin: 0 0 calc(32 / 16 * 1rem) 0;
            padding-left: calc(20 / 16 * 1rem);
            /* prevents padding from affecting width and height */
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            align-items: flex-start;
            gap: calc(8 / 16 * 1rem);
        }

        .cs-li {
            font-size: var(--bodyFontSize);
            line-height: 1.5em;
            list-style: none;
            text-align: left;
            width: 100%;
            max-width: calc(400 / 16 * 1rem);
            margin: 0;
            color: var(--bodyTextColor);
            position: relative;

            &:before {
                /* bullet */
                content: '';
                width: 4px;
                height: 4px;
                border-radius: 50%;
                background: currentColor;
                opacity: 1;
                position: absolute;
                display: block;
                top: calc(10 / 16 * 1rem);
                left: calc(-15 / 16 * 1rem);
            }
        }

        .cs-button-solid {
            font-size: calc(16 / 16 * 1rem);
            /* 46px - 56px */
            line-height: clamp(2.875rem, 5.5vw, 3.5rem);
            text-decoration: none;
	    	font-weight: 700;
	        text-align: center;
            margin: 0;
            color: #fff;
            min-width: calc(200 / 16 * 1rem);
            padding: 0 calc(24 / 16 * 1rem);
            background-color: var(--primary);
            border-radius: calc(8 / 16 * 1rem);
            display: inline-block;
            position: relative;
            z-index: 1;
            /* prevents padding from adding to the width */
            box-sizing: border-box;

            &:before {
                content: '';
                position: absolute;
                height: 100%;
                width: 0%;
                background: #000;
                opacity: 1;
                top: 0;
                left: 0;
                z-index: -1;
                border-radius: calc(8 / 16 * 1rem);
                transition: width .3s;
            }

            &:hover {
                &:before {
                    width: 100%;
                }
            }
        }

        .cs-image-group {
            /* scaling entire section down. font-size starts at a min in vw, and stops when that value reaches 1em (16px). Since we want the picture elements to base their font size on the parent and not the root, we use ems for this entire section  */
            font-size: min(2.18vw, .791em);
            width: calc(662 / 16 * 1em);
            height: calc(756 / 16 * 1em);
            position: relative;
            z-index: 1;
        }

        .cs-picture {
            /* width 100% so we can match the border radius with the circle graphic easier */
            width: 100%;
            height: calc(782 / 16 * 1em);
            display: flex;
            justify-content: center;
            align-items: flex-end;
            /* clips images to the shape of the parent */
            overflow: hidden;
            position: absolute;
            bottom: 0;
            left: 50%;
            z-index: 10;
            transform: translateX(-50%);

            img {
                width: auto;
                height: calc(782 / 16 * 1em);
                position: relative;
                bottom: calc(-18 / 16 * 1em);
                left: 0;
            }
        }

        .cs-picture1 {
            /* this is set to match the curve of the circle graphic behind it */
            border-radius: 0 0 47.8% 47.4%;
        }

        .cs-picture2 {
            /* this is the picture mask, we make the picture element a specific height and width thats smaller than the actual image, and where the actual image overflows the new dimensions it gets cut off by the overflow:hidden.  This crop is the image that overlaps the circle shapes we created.  */
            width: calc(413 / 16 * 1em);
            height: calc(743 / 16 * 1em);
            border-radius: 0 0 calc(19 / 16 * 1em) 0;
            /* place the cropped image exactly over the cs-picture1 to finish the masking effect */
            left: calc(277 / 16 * 1em);
            bottom: calc(21 / 16 * 1em);
            

            img {
                position: absolute;
                top: 0;
                left: 0;
            }
        }

        .cs-graphic {
            width: 100%;
            height: auto;
            position: absolute;
            bottom: 0;
            left: 0;
            z-index: -1;
        }

        .cs-stat {
            text-align: center;
            display: inline-flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            gap: calc(4 / 16 * 1rem);
            position: absolute;
            top: calc(74 / 16 * 1em);
            left: calc(28 / 16 * 1em);
            z-index: 1;
        }

        .cs-number {
            /* 20px - 39px */
            font-size: clamp(1.25rem, 3vw, 2.4375rem);
            line-height: 1.2em;
            font-weight: 700;
            text-align: inherit;
            margin: 0;
            color: var(--bodyTextColorWhite);
        }

        .cs-desc {
            /* 10px - 14px */
            font-size: clamp(.625rem, 1vw, .875rem);
            line-height: 1.5em;
            text-align: inherit;
            width: 100%;
            /* 80px - 148px */
            max-width: clamp(5rem, 11vw, 9.25rem);
            margin: 0;
            color: var(--bodyTextColorWhite);
        }

        .cs-blob {
            width: 142%;
            max-width: calc(200 / 16 * 1rem);
            height: auto;
            position: absolute;
            top: 50%;
            left: 50%;
            z-index: -1;
            transform: translate(-50%, -50%);
        }

        .cs-arrow {
            width: calc(130 / 16 * 1em);
            height: auto;
            position: absolute;
            top: clamp(150%, 14vw, 180%);
            left: calc(5 / 16 * 1em);
            transform: rotate(-52deg);
        }

        .cs-floater {
            /* 270px - 522px */
            width: clamp(16.875rem, 40vw, 32.625rem);
            height: auto;
            position: absolute;
            right: 0;
            bottom: 0;
            z-index: -1;
        }
    }
}

/* Desktop - 1024px */
@media only screen and (min-width: 64rem) {
    #sbs-1123 {
        .cs-container {
            max-width: calc(1280 / 16 * 1rem);
            flex-direction: row;
            align-items: center;
            justify-content: space-between;
        }

        .cs-image-group {
            /* reset the scale */
            font-size: min(1.2vw, 1em);
            /* prevent flexbox from squishing it */
            flex: none;
        }

        .cs-content {
            width: 50%;
        }
    }
}

/* Dark Mode */
@media only screen and (min-width: 0rem) {
    body.dark-mode {
        #sbs-1123 {
            background-color: rgba(0, 0, 0, .2);
            .cs-topper {
                color: var(--secondary);
            }

            .cs-title, .cs-text, .cs-li {
                color: var(--bodyTextColorWhite);
            }

            .cs-floater {
                opacity: .2;
            }
        }
    }
}
                                