<!-- ============================================ -->
<!--                    Hero                      -->
<!-- ============================================ -->

<section id="hero-446">
    <div class="cs-container">
        <div class="cs-content">
            <span class="cs-tag">Tag Example</span>
            <h1 class="cs-title">Solutions we crafted to make human lives easier.</h1>
            <p class="cs-text">
                Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut.
            </p>
        </div>
        <!--Stats Section-->
        <ul class="cs-card-group">
            <li class="cs-item">
                <span class="cs-number">18k+</span>
                <span class="cs-desc">Satisfied Customers in Bartown, MA</span>
            </li>
            <li class="cs-item">
                <span class="cs-number">5/5</span>
                <span class="cs-desc">Google and Yelp rating</span>
            </li>
            <li class="cs-item">
                <span class="cs-number">100%</span>
                <span class="cs-desc">Satisfaction guaranteed on every project</span>
            </li>
        </ul>
    </div>
    <!-- Background Image -->
    <picture class="cs-background">
        <source media="(max-width: 600px)" srcset="https://csimg.nyc3.cdn.digitaloceanspaces.com/Hero/leaf-bg-m.jpg">
        <source media="(min-width: 601px)" srcset="https://csimg.nyc3.cdn.digitaloceanspaces.com/Hero/leaf-bg.jpg">
        <img loading="lazy" decoding="async" src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Hero/leaf-bg.jpg" alt="leaf" width="1920" height="1000" aria-hidden="true">
    </picture>
</section>
                                
/*-- -------------------------- -->
<---           Hero             -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
  :root {
    /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
    --primary: #1F861D;
    --primaryLight: #001F3F;
    --secondary: #FFBA43;
    --secondaryLight: #FFBA43;
    --headerColor: #1a1a1a;
    --bodyTextColor: #4E4B66;
    --bodyTextColorWhite: #FAFBFC;
    /* 13px - 16px */
    --topperFontSize: clamp(0.8125rem, 1.6vw, 1rem);
    /* 31px - 49px */
    --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
    --bodyFontSize: 1rem;
    /* 60px - 100px top and bottom */
    --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
  }
  #hero-446 {
    /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
    font-family: 'Roboto', 'Arial', sans-serif;
    /* Centers tag */
    text-align: center;
    /* 144px - 300px - leaving extra space for the navigation */
    padding: clamp(9rem, 25.95vw, 19.75rem) 1rem 0;
    padding-bottom: 0;
    position: relative;
    z-index: 1;
  }
  #hero-446 .cs-background {
    width: 100%;
    height: 100%;
    display: block;
    position: absolute;
    top: 0;
    left: 0;
    z-index: -2;
  }
  #hero-446 .cs-background:before {
    /* Overlay */
    content: '';
    width: 100%;
    height: 100%;
    background: #1A1A1A;
    mix-blend-mode: multiply;
    opacity: 0.4;
    position: absolute;
    display: block;
    top: 0;
    left: 0;
    z-index: 1;
    /* prevents the cursor from interacting with it */
    pointer-events: none;
  }
  #hero-446 .cs-background img {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width: 100%;
    object-fit: cover;
  }
  #hero-446 .cs-container {
    width: 100%;
    max-width: 80rem;
    margin: auto;
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    align-items: center;
    /* 80px - 120px */
    gap: clamp(5rem, 10vw, 7.5rem);
  }
  #hero-446 .cs-content {
    margin: auto;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
  }
  #hero-446 .cs-tag {
    /* 14px - 16px */
    font-size: clamp(0.875rem, 1.5vw, 1rem);
    /* 30px - 44px */
    line-height: clamp(1.875rem, 4vw, 2.75rem);
    text-align: center;
    text-transform: uppercase;
    /* 12px - 20px */
    padding: 0 clamp(0.75rem, 1.6vw, 1.25rem);
    /* 12px - 20px */
    margin-bottom: clamp(0.75rem, 2vw, 1.25rem);
    color: var(--bodyTextColorWhite);
    border: 1px solid var(--bodyTextColorWhite);
    background: rgba(250, 251, 252, 0.2);
    -webkit-backdrop-filter: blur(6px);
    backdrop-filter: blur(6px);
    border-radius: 1.875rem;
    /* prevents border from affecting height and width */
    box-sizing: border-box;
    display: inline-block;
  }
  #hero-446 .cs-title {
    /* 31px - 61px */
    font-size: clamp(1.9375rem, 6.4vw, 3.8125rem);
    font-weight: 700;
    line-height: 1.2em;
    text-align: center;
    max-width: 51.8125rem;
    /* 16px - 24px */
    margin: 0 auto clamp(1rem, 4vw, 1.5rem) 0;
    color: var(--bodyTextColorWhite);
    position: relative;
  }
  #hero-446 .cs-text {
    /* 16px - 20px */
    font-size: clamp(1rem, 1.95vw, 1.25rem);
    line-height: 1.5em;
    text-align: center;
    width: 100%;
    /* 464px - 622px */
    max-width: clamp(29rem, 60vw, 38.785rem);
    margin: 0;
    color: var(--bodyTextColorWhite);
  }
  #hero-446 .cs-card-group {
    width: 100%;
    padding: 0;
    margin: 0;
    background-color: #fff;
    filter: drop-shadow(0px 24px 54px rgba(87, 107, 147, 0.12));
    /* 12px - 24px */
    border-radius: clamp(0.75rem, 2vw, 1.5rem) clamp(0.75rem, 2vw, 1.5rem) 0 0;
    display: flex;
    justify-content: center;
    align-items: stretch;
    flex-wrap: wrap;
  }
  #hero-446 .cs-item {
    list-style: none;
    width: 50%;
    margin: 0;
    /* 16px - 48px top and bottom */
    /* 12px - 110px top and bottom */
    padding: clamp(1rem, 3.7vw, 3rem) clamp(0.75rem, 3vw, 6.875rem);
    /* prevents padding from affecting height and width */
    box-sizing: border-box;
    border-right: 1px solid #dcdcdd;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
  }
  #hero-446 .cs-item:nth-of-type(2) {
    border: none;
  }
  #hero-446 .cs-item:last-of-type {
    width: 100%;
    border-top: 1px solid #dcdcdd;
    border-right: none;
  }
  #hero-446 .cs-number {
    /* 31px - 61px */
    font-size: clamp(1.9375rem, 4vw, 3.8125rem);
    line-height: 1.2em;
    font-weight: 700;
    text-align: center;
    color: var(--primary);
    margin: 0 0 0.25rem 0;
    display: block;
  }
  #hero-446 .cs-desc {
    /* 14px - 16px */
    font-size: clamp(0.875rem, 1.5vw, 1rem);
    line-height: 1.2em;
    font-weight: 400;
    text-align: center;
    max-width: 12.5rem;
    margin: 0;
    color: var(--bodyTextColor);
    display: block;
  }
}
/* In-Between - 650px */
@media only screen and (min-width: 40.625rem) {
  #hero-446 .cs-card-group {
    flex-wrap: nowrap;
  }
  #hero-446 .cs-item {
    width: 33.33%;
  }
  #hero-446 .cs-item:nth-of-type(2) {
    border-right: 1px solid #dcdcdd;
  }
  #hero-446 .cs-item:last-of-type {
    width: 33.33%;
    border: none;
  }
}
                                
/*-- -------------------------- -->
<---           Hero             -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
  :root {
    /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
    --primary: #1F861D;
    --primaryLight: #001F3F;
    --secondary: #FFBA43;
    --secondaryLight: #FFBA43;
    --headerColor: #1a1a1a;
    --bodyTextColor: #4E4B66;
    --bodyTextColorWhite: #FAFBFC;
    /* 13px - 16px */
    --topperFontSize: clamp(0.8125rem, 1.6vw, 1rem);
    /* 31px - 49px */
    --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
    --bodyFontSize: 1rem;
    /* 60px - 100px top and bottom */
    --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
  }
  #hero-446 {
    /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
    font-family: 'Roboto', 'Arial', sans-serif;
    /* Centers tag */
    text-align: center;
    /* 144px - 300px - leaving extra space for the navigation */
    padding: clamp(9rem, 25.95vw, 19.75rem) 1rem 0;
    padding-bottom: 0;
    position: relative;
    z-index: 1;
  }
  #hero-446 .cs-background {
    width: 100%;
    height: 100%;
    display: block;
    position: absolute;
    top: 0;
    left: 0;
    z-index: -2;
  }
  #hero-446 .cs-background:before {
    /* Overlay */
    content: '';
    width: 100%;
    height: 100%;
    background: #1A1A1A;
    mix-blend-mode: multiply;
    opacity: 0.4;
    position: absolute;
    display: block;
    top: 0;
    left: 0;
    z-index: 1;
    /* prevents the cursor from interacting with it */
    pointer-events: none;
  }
  #hero-446 .cs-background img {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width: 100%;
    object-fit: cover;
  }
  #hero-446 .cs-container {
    width: 100%;
    max-width: 80rem;
    margin: auto;
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    align-items: center;
    /* 80px - 120px */
    gap: clamp(5rem, 10vw, 7.5rem);
  }
  #hero-446 .cs-content {
    margin: auto;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
  }
  #hero-446 .cs-tag {
    /* 14px - 16px */
    font-size: clamp(0.875rem, 1.5vw, 1rem);
    /* 30px - 44px */
    line-height: clamp(1.875rem, 4vw, 2.75rem);
    text-align: center;
    text-transform: uppercase;
    /* 12px - 20px */
    padding: 0 clamp(0.75rem, 1.6vw, 1.25rem);
    /* 12px - 20px */
    margin-bottom: clamp(0.75rem, 2vw, 1.25rem);
    color: var(--bodyTextColorWhite);
    border: 1px solid var(--bodyTextColorWhite);
    background: rgba(250, 251, 252, 0.2);
    -webkit-backdrop-filter: blur(6px);
    backdrop-filter: blur(6px);
    border-radius: 1.875rem;
    /* prevents border from affecting height and width */
    box-sizing: border-box;
    display: inline-block;
  }
  #hero-446 .cs-title {
    /* 31px - 61px */
    font-size: clamp(1.9375rem, 6.4vw, 3.8125rem);
    font-weight: 700;
    line-height: 1.2em;
    text-align: center;
    max-width: 51.8125rem;
    /* 16px - 24px */
    margin: 0 auto clamp(1rem, 4vw, 1.5rem) 0;
    color: var(--bodyTextColorWhite);
    position: relative;
  }
  #hero-446 .cs-text {
    /* 16px - 20px */
    font-size: clamp(1rem, 1.95vw, 1.25rem);
    line-height: 1.5em;
    text-align: center;
    width: 100%;
    /* 464px - 622px */
    max-width: clamp(29rem, 60vw, 38.785rem);
    margin: 0;
    color: var(--bodyTextColorWhite);
  }
  #hero-446 .cs-card-group {
    width: 100%;
    padding: 0;
    margin: 0;
    background-color: #fff;
    filter: drop-shadow(0px 24px 54px rgba(87, 107, 147, 0.12));
    /* 12px - 24px */
    border-radius: clamp(0.75rem, 2vw, 1.5rem) clamp(0.75rem, 2vw, 1.5rem) 0 0;
    display: flex;
    justify-content: center;
    align-items: stretch;
    flex-wrap: wrap;
  }
  #hero-446 .cs-item {
    list-style: none;
    width: 50%;
    margin: 0;
    /* 16px - 48px top and bottom */
    /* 12px - 110px top and bottom */
    padding: clamp(1rem, 3.7vw, 3rem) clamp(0.75rem, 3vw, 6.875rem);
    /* prevents padding from affecting height and width */
    box-sizing: border-box;
    border-right: 1px solid #dcdcdd;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
  }
  #hero-446 .cs-item:nth-of-type(2) {
    border: none;
  }
  #hero-446 .cs-item:last-of-type {
    width: 100%;
    border-top: 1px solid #dcdcdd;
    border-right: none;
  }
  #hero-446 .cs-number {
    /* 31px - 61px */
    font-size: clamp(1.9375rem, 4vw, 3.8125rem);
    line-height: 1.2em;
    font-weight: 700;
    text-align: center;
    color: var(--primary);
    margin: 0 0 0.25rem 0;
    display: block;
  }
  #hero-446 .cs-desc {
    /* 14px - 16px */
    font-size: clamp(0.875rem, 1.5vw, 1rem);
    line-height: 1.2em;
    font-weight: 400;
    text-align: center;
    max-width: 12.5rem;
    margin: 0;
    color: var(--bodyTextColor);
    display: block;
  }
}
/* In-Between - 650px */
@media only screen and (min-width: 40.625rem) {
  #hero-446 .cs-card-group {
    flex-wrap: nowrap;
  }
  #hero-446 .cs-item {
    width: 33.33%;
  }
  #hero-446 .cs-item:nth-of-type(2) {
    border-right: 1px solid #dcdcdd;
  }
  #hero-446 .cs-item:last-of-type {
    width: 33.33%;
    border: none;
  }
}
/* Dark Mode */
@media only screen and (min-width: 0rem) {
  body.dark-mode #hero-446 .cs-background:before {
    opacity: .85;
  }
  body.dark-mode #hero-446 .cs-card-group {
    background-color: var(--accent);
  }
  body.dark-mode #hero-446 .cs-number,
  body.dark-mode #hero-446 .cs-desc {
    color: var(--bodyTextColorWhite);
  }
}
                                