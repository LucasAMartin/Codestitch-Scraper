<!-- ============================================ -->
<!--                   Contact                    -->
<!-- ============================================ -->

<section id="contact-1092">
    <div class="cs-container">
        <div class="cs-form-group">
            <!--Form-->
            <form class="cs-form" id="cs-form-1092" name="Contact Form" method="post">
                <div class="cs-form-content">
                    <span class="cs-topper">Contact Us</span>
                    <h2 class="cs-title">Make Appointment</h2>
                </div>
                <label class="cs-label">
                    Name
                    <input class="cs-input" required type="text" id="name-1092" name="name" placeholder="Name">
                </label>
                <label class="cs-label">
                    Phone
                    <input class="cs-input" required type="number" id="phone-1092" name="phone" placeholder="Phone">
                </label>
                <label class="cs-label">
                    Email
                    <input class="cs-input" required type="email" id="email-1092" name="email" placeholder="Email">
                </label>
                <label class="cs-label cs-message">
                    Message
                    <textarea class="cs-input cs-textarea" required name="Message" id="message-1092" placeholder="Write message..."></textarea>
                </label>
                <button class="cs-button-solid cs-submit" type="submit">Make An Appointment</button>
            </form>
            <!--Background image, background image set in CSS at 1500px-->
            <picture class="cs-background">
                <!--Mobile Image-->
                <source media="(max-width: 600px)" srcset="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/People/mechaninc16.jpg">
                <!--Tablet and above Image-->
                <source media="(min-width: 601px)" srcset="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/People/mechaninc16.jpg">
                <img loading="lazy" decoding="async" src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/People/mechaninc16.jpg" alt="profile" width="841" height="877">
            </picture>
        </div>
        <div class="cs-content">
            <img class="cs-icon" loading="lazy" decoding="async" src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Icons/red-quotes.svg" alt="quotes" width="73" height="60" aria-hidden="true">
            <span class="cs-topper">Testimonials</span>
            <h2 class="cs-title">Trusted by Thousand of People 
                & Companies</h2>
            <p class="cs-text">
                Lorem ipsum dolor sit, amet consectetur adipisicing elit. Obcaecati similique recusandae sequi quas nobis error voluptatum, corrupti in natus laborum vel dignissimos alias. Voluptate provident maiores, ducimus amet repellat optio.
            </p>
            <div class="cs-review-group">
                <picture class="cs-picture">
                    <!--Mobile Image-->
                    <source media="(max-width: 600px)" srcset="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/People/person9.jpg">
                    <!--Tablet and above Image-->
                    <source media="(min-width: 601px)" srcset="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/People/person9.jpg">
                    <img loading="lazy" decoding="async" src="https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/People/person9.jpg" alt="room" width="179" height="202">
                </picture>
                <div class="cs-flex">
                    <p class="cs-review-text">
                        When you think about collecting customer feed back, it’s easy to get overwhelmed by the sheer volume of . With so many more customers and so many ways to connect with their feedback it’s hard to know to where to start.
                    </p>
                    <span class="cs-name">George Thomas</span>
                    <span class="cs-desc">Founder Of Yokoloko</span>
                </div>
            </div>
        </div>
    </div>
</section>
                                
/*-- -------------------------- -->
<---          Contact           -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
  :root {
    /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
    --primary: #D90429;
    --primaryLight: #d8673d;
    --secondary: #FFBA43;
    --secondaryLight: #FFBA43;
    --headerColor: #1a1a1a;
    --bodyTextColor: #4E4B66;
    --bodyTextColorWhite: #FAFBFC;
    /* 13px - 16px */
    --topperFontSize: clamp(0.8125rem, 1.6vw, 1rem);
    /* 31px - 49px */
    --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
    --bodyFontSize: 1rem;
    /* 60px - 100px top and bottom */
    --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
  }
  #contact-1092 {
    /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
    font-family: 'Roboto', 'Arial', sans-serif;
    background-color: #f7f7f7;
    position: relative;
    z-index: 1;
  }
  #contact-1092 .cs-container {
    width: 100%;
    /* chnaged to 1280px at large desktop */
    max-width: 96.875rem;
    margin: auto;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
  }
  #contact-1092 .cs-content {
    /* set text aling to left if content needs to be left aligned */
    text-align: left;
    width: 100%;
    max-width: 49rem;
    padding: var(--sectionPadding);
    /* prevents padding from affecting height and width */
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    /* centers content horizontally, set to flex-start to left align */
    align-items: flex-start;
    /* prevents flexbox from squishing it */
    flex: none;
  }
  #contact-1092 .cs-topper {
    font-size: var(--topperFontSize);
    line-height: 1.2em;
    text-transform: uppercase;
    text-align: inherit;
    letter-spacing: .1em;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 0.25rem;
    display: block;
  }
  #contact-1092 .cs-title {
    font-size: var(--headerFontSize);
    font-weight: 900;
    line-height: 1.2em;
    text-align: inherit;
    max-width: 43.75rem;
    margin: 0 0 1rem 0;
    color: var(--headerColor);
    position: relative;
  }
  #contact-1092 .cs-text {
    font-size: var(--bodyFontSize);
    line-height: 1.5em;
    text-align: inherit;
    width: 100%;
    max-width: 40.625rem;
    margin: 0;
    color: var(--bodyTextColor);
  }
  #contact-1092 .cs-topper {
    /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
    color: #767676;
  }
  #contact-1092 .cs-title {
    /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
    max-width: 20ch;
  }
  #contact-1092 .cs-text {
    /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
    margin-bottom: 1rem;
  }
  #contact-1092 .cs-text:last-of-type {
    margin-bottom: 2rem;
  }
  #contact-1092 .cs-form-group {
    width: 100%;
    /* moved section padding to the content so the picture can be full width */
    padding: var(--sectionPadding);
    /* prevents padding from affecting height and width */
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 3rem;
    position: relative;
  }
  #contact-1092 .cs-form-content {
    /* set text aling to left if content needs to be left aligned */
    text-align: left;
    width: 100%;
    display: flex;
    flex-direction: column;
    /* centers content horizontally, set to flex-start to left align */
    align-items: flex-start;
  }
  #contact-1092 .cs-form-content .cs-topper {
    color: var(--secondary);
  }
  #contact-1092 .cs-form-content .cs-title {
    color: var(--bodyTextColorWhite);
    /* 36px - 48px */
    margin-bottom: clamp(2.25rem, 4vw, 3rem);
  }
  #contact-1092 .cs-form {
    width: 100%;
    max-width: 49rem;
    /* 32px - 48px top & bottom */
    /* 16px - 48px left & right */
    padding: clamp(2rem, 4vw, 3rem) clamp(1rem, 4vw, 3rem);
    /* prevents padding from affecting height and width */
    box-sizing: border-box;
    background-color: rgba(26, 26, 26, 0.72);
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.75rem;
  }
  #contact-1092 .cs-label {
    /* 14px - 16px */
    font-size: clamp(0.875rem, 1.5vw, 1rem);
    width: 100%;
    color: var(--bodyTextColorWhite);
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    gap: 0.25rem;
  }
  #contact-1092 .cs-input {
    font-size: 1rem;
    width: 100%;
    height: 3.5rem;
    padding: 0;
    padding-left: 1.5rem;
    color: var(--bodyTextColorWhite);
    background-color: #595756;
    border: none;
    /* prevents padding from adding to height and width */
    box-sizing: border-box;
    transition: border-color 0.3s;
  }
  #contact-1092 .cs-input:hover {
    border-color: var(--secondary);
  }
  #contact-1092 .cs-input:focus {
    outline: 1px solid var(--secondary);
  }
  #contact-1092 .cs-input::placeholder {
    color: #BABABA;
    opacity: .6;
  }
  #contact-1092 .cs-textarea {
    min-height: 7.5rem;
    padding-top: 1.5rem;
    margin: 0;
    font-family: inherit;
  }
  #contact-1092 .cs-button-solid {
    font-size: 1rem;
    /* 46px - 56px */
    line-height: clamp(2.875rem, 5.5vw, 3.5rem);
    text-decoration: none;
    font-weight: 700;
    text-align: center;
    margin: 0;
    color: #fff;
    min-width: 9.375rem;
    padding: 0 1.5rem;
    background-color: var(--primary);
    border-radius: 0.25rem;
    display: inline-block;
    position: relative;
    z-index: 1;
    /* prevents padding from adding to the width */
    box-sizing: border-box;
  }
  #contact-1092 .cs-button-solid:before {
    content: '';
    position: absolute;
    height: 100%;
    width: 0%;
    background: #000;
    opacity: 1;
    top: 0;
    left: 0;
    z-index: -1;
    transition: width .3s;
  }
  #contact-1092 .cs-button-solid:hover:before {
    width: 100%;
  }
  #contact-1092 .cs-submit {
    min-width: 15.0625rem;
    /* 36px - 48px */
    margin-top: clamp(2.25rem, 4vw, 3rem);
    border: none;
    border-radius: 0;
  }
  #contact-1092 .cs-submit:hover {
    cursor: pointer;
  }
  #contact-1092 .cs-background {
    width: 100%;
    height: 100%;
    display: block;
    position: absolute;
    top: 0;
    left: 0;
    z-index: -1;
  }
  #contact-1092 .cs-background:before {
    /* background color overlay */
    content: '';
    position: absolute;
    display: block;
    height: 100%;
    width: 100%;
    background: #1a1a1a;
    opacity: .72;
    top: 0;
    left: 0;
    z-index: 1;
  }
  #contact-1092 .cs-background img {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width: 100%;
    /* Makes img tag act as a background image */
    object-fit: cover;
  }
  #contact-1092 .cs-icon {
    /* 48px - 72px */
    width: clamp(3rem, 6vw, 4.5rem);
    height: auto;
    /* 28px - 40px */
    margin: 0 0 clamp(1.75rem, 4vw, 2.5rem);
    display: block;
  }
  #contact-1092 .cs-review-group {
    /* 24px - 32px top & bottom */
    /* 16px - 32px left & right */
    padding: clamp(1.5rem, 4vw, 2rem) clamp(1rem, 5vw, 2rem);
    /* prevents padding from affecting height and width */
    box-sizing: border-box;
    background-color: #fff;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: clamp(1.75rem, 4vw, 2.5rem);
  }
  #contact-1092 .cs-picture {
    /* dimensions change at tablet */
    width: 100%;
    height: 15rem;
    /* prevents flexbox from squishing it */
    flex: none;
  }
  #contact-1092 .cs-picture img {
    width: 100%;
    max-width: 18.75rem;
    height: 100%;
    object-fit: cover;
    /* places the top of the image to the top of the parent */
    object-position: top;
  }
  #contact-1092 .cs-review-text {
    /* 14px - 16px */
    font-size: clamp(0.875rem, 1.5vw, 1rem);
    line-height: 1.5em;
    text-align: left;
    /* 16px - 28px */
    margin: 0 0 clamp(1rem, 3vw, 1.75rem);
    color: var(--bodyTextColor);
  }
  #contact-1092 .cs-name {
    /* 20px - 25px */
    font-size: clamp(1.25rem, 3vw, 1.5625rem);
    line-height: 1.2em;
    font-weight: bold;
    text-align: left;
    margin: 0;
    display: block;
  }
  #contact-1092 .cs-desc {
    /* 14px - 16px */
    font-size: clamp(0.875rem, 1.5vw, 1rem);
    line-height: 1.5em;
    text-align: left;
    margin: 0;
    color: var(--bodyTextColor);
    display: block;
  }
}
/* Tablet - 768px */
@media only screen and (min-width: 48rem) {
  #contact-1092 .cs-review-group {
    flex-direction: row;
    justify-content: space-between;
  }
  #contact-1092 .cs-picture {
    width: 11.1875rem;
    height: 12.625rem;
  }
}
/* Large Desktop - 1500px */
@media only screen and (min-width: 93.75rem) {
  #contact-1092 {
    padding: var(--sectionPadding);
    position: relative;
  }
  #contact-1092 .cs-container {
    max-width: 80rem;
    flex-direction: row;
    justify-content: space-between;
    gap: 5.625rem;
  }
  #contact-1092 .cs-content {
    max-width: 39.375rem;
    padding: 0;
  }
  #contact-1092 .cs-form-group {
    padding: 0;
    /* undo the position, so the cs-background will position itself relative to the next parent that has a decalred position relative, which we added to the #content parent div. Now we can position it relative to the whole section, allowing us to have more control on how it covers the section. */
    position: initial;
  }
  #contact-1092 .cs-form {
    transform: translateX(-5rem);
  }
  #contact-1092 .cs-background {
    width: 50vw;
    margin-right: 5rem;
    right: 50%;
    left: auto;
    background: url("https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/People/mechaninc16.jpg");
    background-size: cover;
    background-position: right center;
    background-repeat: no-repeat;
    /* makes a parallax scroll effect */
    background-attachment: fixed;
  }
  #contact-1092 .cs-background img {
    display: none;
  }
}
                                
/*-- -------------------------- -->
<---          Contact           -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
  :root {
    /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
    --primary: #D90429;
    --primaryLight: #d8673d;
    --secondary: #FFBA43;
    --secondaryLight: #FFBA43;
    --headerColor: #1a1a1a;
    --bodyTextColor: #4E4B66;
    --bodyTextColorWhite: #FAFBFC;
    /* 13px - 16px */
    --topperFontSize: clamp(0.8125rem, 1.6vw, 1rem);
    /* 31px - 49px */
    --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
    --bodyFontSize: 1rem;
    /* 60px - 100px top and bottom */
    --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
  }
  #contact-1092 {
    /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
    font-family: 'Roboto', 'Arial', sans-serif;
    background-color: #f7f7f7;
    position: relative;
    z-index: 1;
  }
  #contact-1092 .cs-container {
    width: 100%;
    /* chnaged to 1280px at large desktop */
    max-width: 96.875rem;
    margin: auto;
    display: flex;
    justify-content: center;
    align-items: center;
    flex-direction: column;
  }
  #contact-1092 .cs-content {
    /* set text aling to left if content needs to be left aligned */
    text-align: left;
    width: 100%;
    max-width: 49rem;
    padding: var(--sectionPadding);
    /* prevents padding from affecting height and width */
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    /* centers content horizontally, set to flex-start to left align */
    align-items: flex-start;
    /* prevents flexbox from squishing it */
    flex: none;
  }
  #contact-1092 .cs-topper {
    font-size: var(--topperFontSize);
    line-height: 1.2em;
    text-transform: uppercase;
    text-align: inherit;
    letter-spacing: .1em;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 0.25rem;
    display: block;
  }
  #contact-1092 .cs-title {
    font-size: var(--headerFontSize);
    font-weight: 900;
    line-height: 1.2em;
    text-align: inherit;
    max-width: 43.75rem;
    margin: 0 0 1rem 0;
    color: var(--headerColor);
    position: relative;
  }
  #contact-1092 .cs-text {
    font-size: var(--bodyFontSize);
    line-height: 1.5em;
    text-align: inherit;
    width: 100%;
    max-width: 40.625rem;
    margin: 0;
    color: var(--bodyTextColor);
  }
  #contact-1092 .cs-topper {
    /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
    color: #767676;
  }
  #contact-1092 .cs-title {
    /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
    max-width: 20ch;
  }
  #contact-1092 .cs-text {
    /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
    margin-bottom: 1rem;
  }
  #contact-1092 .cs-text:last-of-type {
    margin-bottom: 2rem;
  }
  #contact-1092 .cs-form-group {
    width: 100%;
    /* moved section padding to the content so the picture can be full width */
    padding: var(--sectionPadding);
    /* prevents padding from affecting height and width */
    box-sizing: border-box;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 3rem;
    position: relative;
  }
  #contact-1092 .cs-form-content {
    /* set text aling to left if content needs to be left aligned */
    text-align: left;
    width: 100%;
    display: flex;
    flex-direction: column;
    /* centers content horizontally, set to flex-start to left align */
    align-items: flex-start;
  }
  #contact-1092 .cs-form-content .cs-topper {
    color: var(--secondary);
  }
  #contact-1092 .cs-form-content .cs-title {
    color: var(--bodyTextColorWhite);
    /* 36px - 48px */
    margin-bottom: clamp(2.25rem, 4vw, 3rem);
  }
  #contact-1092 .cs-form {
    width: 100%;
    max-width: 49rem;
    /* 32px - 48px top & bottom */
    /* 16px - 48px left & right */
    padding: clamp(2rem, 4vw, 3rem) clamp(1rem, 4vw, 3rem);
    /* prevents padding from affecting height and width */
    box-sizing: border-box;
    background-color: rgba(26, 26, 26, 0.72);
    display: flex;
    justify-content: space-between;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.75rem;
  }
  #contact-1092 .cs-label {
    /* 14px - 16px */
    font-size: clamp(0.875rem, 1.5vw, 1rem);
    width: 100%;
    color: var(--bodyTextColorWhite);
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: flex-start;
    gap: 0.25rem;
  }
  #contact-1092 .cs-input {
    font-size: 1rem;
    width: 100%;
    height: 3.5rem;
    padding: 0;
    padding-left: 1.5rem;
    color: var(--bodyTextColorWhite);
    background-color: #595756;
    border: none;
    /* prevents padding from adding to height and width */
    box-sizing: border-box;
    transition: border-color 0.3s;
  }
  #contact-1092 .cs-input:hover {
    border-color: var(--secondary);
  }
  #contact-1092 .cs-input:focus {
    outline: 1px solid var(--secondary);
  }
  #contact-1092 .cs-input::placeholder {
    color: #BABABA;
    opacity: .6;
  }
  #contact-1092 .cs-textarea {
    min-height: 7.5rem;
    padding-top: 1.5rem;
    margin: 0;
    font-family: inherit;
  }
  #contact-1092 .cs-button-solid {
    font-size: 1rem;
    /* 46px - 56px */
    line-height: clamp(2.875rem, 5.5vw, 3.5rem);
    text-decoration: none;
    font-weight: 700;
    text-align: center;
    margin: 0;
    color: #fff;
    min-width: 9.375rem;
    padding: 0 1.5rem;
    background-color: var(--primary);
    border-radius: 0.25rem;
    display: inline-block;
    position: relative;
    z-index: 1;
    /* prevents padding from adding to the width */
    box-sizing: border-box;
  }
  #contact-1092 .cs-button-solid:before {
    content: '';
    position: absolute;
    height: 100%;
    width: 0%;
    background: #000;
    opacity: 1;
    top: 0;
    left: 0;
    z-index: -1;
    transition: width .3s;
  }
  #contact-1092 .cs-button-solid:hover:before {
    width: 100%;
  }
  #contact-1092 .cs-submit {
    min-width: 15.0625rem;
    /* 36px - 48px */
    margin-top: clamp(2.25rem, 4vw, 3rem);
    border: none;
    border-radius: 0;
  }
  #contact-1092 .cs-submit:hover {
    cursor: pointer;
  }
  #contact-1092 .cs-background {
    width: 100%;
    height: 100%;
    display: block;
    position: absolute;
    top: 0;
    left: 0;
    z-index: -1;
  }
  #contact-1092 .cs-background:before {
    /* background color overlay */
    content: '';
    position: absolute;
    display: block;
    height: 100%;
    width: 100%;
    background: #1a1a1a;
    opacity: .72;
    top: 0;
    left: 0;
    z-index: 1;
  }
  #contact-1092 .cs-background img {
    position: absolute;
    top: 0;
    left: 0;
    height: 100%;
    width: 100%;
    /* Makes img tag act as a background image */
    object-fit: cover;
  }
  #contact-1092 .cs-icon {
    /* 48px - 72px */
    width: clamp(3rem, 6vw, 4.5rem);
    height: auto;
    /* 28px - 40px */
    margin: 0 0 clamp(1.75rem, 4vw, 2.5rem);
    display: block;
  }
  #contact-1092 .cs-review-group {
    /* 24px - 32px top & bottom */
    /* 16px - 32px left & right */
    padding: clamp(1.5rem, 4vw, 2rem) clamp(1rem, 5vw, 2rem);
    /* prevents padding from affecting height and width */
    box-sizing: border-box;
    background-color: #fff;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: clamp(1.75rem, 4vw, 2.5rem);
  }
  #contact-1092 .cs-picture {
    /* dimensions change at tablet */
    width: 100%;
    height: 15rem;
    /* prevents flexbox from squishing it */
    flex: none;
  }
  #contact-1092 .cs-picture img {
    width: 100%;
    max-width: 18.75rem;
    height: 100%;
    object-fit: cover;
    /* places the top of the image to the top of the parent */
    object-position: top;
  }
  #contact-1092 .cs-review-text {
    /* 14px - 16px */
    font-size: clamp(0.875rem, 1.5vw, 1rem);
    line-height: 1.5em;
    text-align: left;
    /* 16px - 28px */
    margin: 0 0 clamp(1rem, 3vw, 1.75rem);
    color: var(--bodyTextColor);
  }
  #contact-1092 .cs-name {
    /* 20px - 25px */
    font-size: clamp(1.25rem, 3vw, 1.5625rem);
    line-height: 1.2em;
    font-weight: bold;
    text-align: left;
    margin: 0;
    display: block;
  }
  #contact-1092 .cs-desc {
    /* 14px - 16px */
    font-size: clamp(0.875rem, 1.5vw, 1rem);
    line-height: 1.5em;
    text-align: left;
    margin: 0;
    color: var(--bodyTextColor);
    display: block;
  }
}
/* Tablet - 768px */
@media only screen and (min-width: 48rem) {
  #contact-1092 .cs-review-group {
    flex-direction: row;
    justify-content: space-between;
  }
  #contact-1092 .cs-picture {
    width: 11.1875rem;
    height: 12.625rem;
  }
}
/* Large Desktop - 1500px */
@media only screen and (min-width: 93.75rem) {
  #contact-1092 {
    padding: var(--sectionPadding);
    position: relative;
  }
  #contact-1092 .cs-container {
    max-width: 80rem;
    flex-direction: row;
    justify-content: space-between;
    gap: 5.625rem;
  }
  #contact-1092 .cs-content {
    max-width: 39.375rem;
    padding: 0;
  }
  #contact-1092 .cs-form-group {
    padding: 0;
    /* undo the position, so the cs-background will position itself relative to the next parent that has a decalred position relative, which we added to the #content parent div. Now we can position it relative to the whole section, allowing us to have more control on how it covers the section. */
    position: initial;
  }
  #contact-1092 .cs-form {
    transform: translateX(-5rem);
  }
  #contact-1092 .cs-background {
    width: 50vw;
    margin-right: 5rem;
    right: 50%;
    left: auto;
    background: url("https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/People/mechaninc16.jpg");
    background-size: cover;
    background-position: right center;
    background-repeat: no-repeat;
    /* makes a parallax scroll effect */
    background-attachment: fixed;
  }
  #contact-1092 .cs-background img {
    display: none;
  }
}
/* Dark Mode */
@media only screen and (min-width: 0rem) {
  body.dark-mode #contact-1092 {
    background-color: rgba(0, 0, 0, 0.6);
  }
  body.dark-mode #contact-1092 .cs-topper {
    color: var(--secondary);
  }
  body.dark-mode #contact-1092 .cs-title,
  body.dark-mode #contact-1092 .cs-text,
  body.dark-mode #contact-1092 .cs-label,
  body.dark-mode #contact-1092 .cs-link,
  body.dark-mode #contact-1092 .cs-review-text,
  body.dark-mode #contact-1092 .cs-name,
  body.dark-mode #contact-1092 .cs-desc {
    color: var(--bodyTextColorWhite);
  }
  body.dark-mode #contact-1092 .cs-text,
  body.dark-mode #contact-1092 .cs-desc {
    opacity: .8;
  }
  body.dark-mode #contact-1092 .cs-icon {
    opacity: .5;
  }
  body.dark-mode #contact-1092 .cs-form {
    background-color: rgba(0, 0, 0, 0.84);
  }
  body.dark-mode #contact-1092 .cs-review-group {
    background-color: var(--medium);
  }
  body.dark-mode #contact-1092 .cs-button-solid {
    transition: color .3s;
  }
  body.dark-mode #contact-1092 .cs-button-solid:before {
    background-color: #fff;
  }
  body.dark-mode #contact-1092 .cs-button-solid:hover {
    color: var(--primary);
  }
  body.dark-mode #contact-1092 .cs-background:before {
    opacity: .90;
    background-color: #000;
  }
}
                                
/*-- -------------------------- -->
<---          Contact           -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
    :root {
        /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
        --primary: #D90429;
        --primaryLight: #d8673d;
        --secondary: #FFBA43;
        --secondaryLight: #FFBA43;
        --headerColor: #1a1a1a;
        --bodyTextColor: #4E4B66;
		--bodyTextColorWhite: #FAFBFC;

        /* 13px - 16px */
        --topperFontSize: clamp(.8125rem, 1.6vw, 1rem);
        /* 31px - 49px */
        --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
        --bodyFontSize: 1rem;

        /* 60px - 100px top and bottom */
        --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
    }

    #contact-1092 {
        /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
        font-family: 'Roboto', 'Arial', sans-serif;
        background-color: #f7f7f7;
        position: relative;
        z-index: 1;
        
        .cs-container {
            width: 100%;
            /* chnaged to 1280px at large desktop */
            max-width: (1550/16rem);
            margin: auto;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }

        .cs-content {
            /* set text aling to left if content needs to be left aligned */
            text-align: left;
            width: 100%;
            max-width: (784/16rem);
            padding: var(--sectionPadding);
            /* prevents padding from affecting height and width */
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            /* centers content horizontally, set to flex-start to left align */
            align-items: flex-start;
            /* prevents flexbox from squishing it */
            flex: none;
        }
    
        .cs-topper {
            font-size: var(--topperFontSize);
            line-height: 1.2em;
            text-transform: uppercase;
            text-align: inherit;
            letter-spacing: .1em;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: (4/16rem);
            display: block;
        }
    
        .cs-title {
            font-size: var(--headerFontSize);
            font-weight: 900;
            line-height: 1.2em;
            text-align: inherit;
            max-width: (700/16rem);
            margin: 0 0 (16/16rem) 0;
            color: var(--headerColor);
            position: relative;
        }
    
        .cs-text {
            font-size: var(--bodyFontSize);
            line-height: 1.5em;
            text-align: inherit;
            width: 100%;
            max-width: (650/16rem);
            margin: 0;
            color: var(--bodyTextColor);
        }

        .cs-topper {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            color: #767676;
        }

        .cs-title {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            max-width: 20ch;
        }

        .cs-text {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            margin-bottom: (16/16rem);
            &:last-of-type {
                margin-bottom: (32/16rem);
            }
        }

        .cs-form-group {
            width: 100%;
            /* moved section padding to the content so the picture can be full width */
            padding: var(--sectionPadding);
            /* prevents padding from affecting height and width */
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: (48/16rem);
            position: relative;
        }

        .cs-form-content {
            /* set text aling to left if content needs to be left aligned */
            text-align: left;
            width: 100%;
            display: flex;
            flex-direction: column;
            /* centers content horizontally, set to flex-start to left align */
            align-items: flex-start;

            .cs-topper {
                color: var(--secondary);
            }

            .cs-title {
                color: var(--bodyTextColorWhite);
                /* 36px - 48px */
                margin-bottom: clamp(2.25rem, 4vw, 3rem);
            }
        }

        .cs-form {
            width: 100%;
            max-width: (784/16rem);
            /* 32px - 48px top & bottom */
            /* 16px - 48px left & right */
            padding: clamp(2rem, 4vw, 3rem) clamp(1rem, 4vw, 3rem);
            /* prevents padding from affecting height and width */
            box-sizing: border-box;
            background-color: rgba(26, 26, 26, .72);
            display: flex;
            justify-content: space-between;
            flex-wrap: wrap;
            align-items: center;
            gap: (12/16rem);
        }

        .cs-label {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.5vw, 1rem);
            width: 100%;
            color: var(--bodyTextColorWhite);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: flex-start;
            gap: (4/16rem);
        }

        .cs-input {
            font-size: (16/16rem);
            width: 100%;
            height: (56/16rem);
            padding: 0;
            padding-left: (24/16rem);
            color: var(--bodyTextColorWhite);
            background-color: #595756;
            border: none;
            /* prevents padding from adding to height and width */
            box-sizing: border-box;
            transition: border-color .3s;
            &:hover {
                border-color: var(--secondary);
            }
            &:focus {
                outline: 1px solid var(--secondary);
            }
            &::placeholder {
                color: #BABABA;
                opacity: .6;
            }
        }

        .cs-textarea {
            min-height: (120/16rem);
            padding-top: (24/16rem);
            margin: 0;
            font-family: inherit;
        }

        .cs-button-solid {
            font-size: (16/16rem);
            /* 46px - 56px */
            line-height: clamp(2.875rem, 5.5vw, 3.5rem);
            text-decoration: none;
	    	font-weight: 700;
	        text-align: center;
            margin: 0;
            color: #fff;
            min-width: (150/16rem);
            padding: 0 (24/16rem);
            background-color: var(--primary);
            border-radius: (4/16rem);
            display: inline-block;
            position: relative;
            z-index: 1;
            /* prevents padding from adding to the width */
            box-sizing: border-box;

            &:before {
                content: '';
                position: absolute;
                height: 100%;
                width: 0%;
                background: #000;
                opacity: 1;
                top: 0;
                left: 0;
                z-index: -1;
                transition: width .3s;
            }

            &:hover {
                &:before {
                    width: 100%;
                }
            }
        }

        .cs-submit {
            min-width: (241/16rem);
            /* 36px - 48px */
            margin-top: clamp(2.25rem, 4vw, 3rem);
            border: none;
            border-radius: 0;

            &:hover {
                cursor: pointer;
            }
        }

        .cs-background {
            width: 100%;
            height: 100%;
            display: block;
            position: absolute;
            top: 0;
            left: 0;
            z-index: -1;

            &:before {
                /* background color overlay */
                content: '';
                position: absolute;
                display: block;
                height: 100%;
                width: 100%;
                background: #1a1a1a;
                opacity: .72;
                top: 0;
                left: 0;
                z-index: 1;
            }

            img {
                position: absolute;
                top: 0;
                left: 0;
                height: 100%;
                width: 100%;
                /* Makes img tag act as a background image */
                object-fit: cover;
            }
        }

        .cs-icon {
            /* 48px - 72px */
            width: clamp(3rem, 6vw, 4.5rem);
            height: auto;
            /* 28px - 40px */
            margin: 0 0 clamp(1.75rem, 4vw, 2.5rem);
            display: block;
        }

        .cs-review-group {
            /* 24px - 32px top & bottom */
            /* 16px - 32px left & right */
            padding: clamp(1.5rem, 4vw, 2rem) clamp(1rem, 5vw, 2rem);
            /* prevents padding from affecting height and width */
            box-sizing: border-box;
            background-color: #fff;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            // /* 28px 40px *
            gap: clamp(1.75rem, 4vw, 2.5rem);
        }

        .cs-picture {
            /* dimensions change at tablet */
            width: 100%;
            height: (240/16rem);
            /* prevents flexbox from squishing it */
            flex: none;

            img {
                width: 100%;
                max-width: (300/16rem);
                height: 100%;
                object-fit: cover;
                /* places the top of the image to the top of the parent */
                object-position: top;
            }
        }

        .cs-review-text {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.5vw, 1rem);
            line-height: 1.5em;
            text-align: left;
            /* 16px - 28px */
            margin: 0 0 clamp(1rem, 3vw, 1.75rem);
            color: var(--bodyTextColor);
        }

        .cs-name {
            /* 20px - 25px */
            font-size: clamp(1.25rem, 3vw, 1.5625rem);
            line-height: 1.2em;
            font-weight: bold;
            text-align: left;
            margin: 0;
            display: block;
        }

        .cs-desc {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.5vw, 1rem);
            line-height: 1.5em;
            text-align: left;
            margin: 0;
            color: var(--bodyTextColor);
            display: block;
        }
    }
}

/* Tablet - 768px */
@media only screen and (min-width: 48rem) {
    #contact-1092 {
        .cs-review-group {
            flex-direction: row;
            justify-content: space-between;
        }

        .cs-picture {
            width: (179/16rem);
            height: (202/16rem);
        }
    }
}

/* Large Desktop - 1500px */
@media only screen and (min-width: 93.75rem) {
    #contact-1092 {
        padding: var(--sectionPadding);
        position: relative;

        .cs-container {
            max-width: (1280/16rem);
            flex-direction: row;
            justify-content: space-between;
            gap: (90/16rem);
        }

        .cs-content {
            max-width: (630/16rem);
            padding: 0;
        }

        .cs-form-group {
            padding: 0;
            /* undo the position, so the cs-background will position itself relative to the next parent that has a decalred position relative, which we added to the #content parent div. Now we can position it relative to the whole section, allowing us to have more control on how it covers the section. */
            position: initial;
        }

        .cs-form {
            transform: translateX(-80/16rem);
        }

        .cs-background {
            width: 50vw;
            margin-right: (80/16rem);
            right: 50%;
            left: auto;
            background: url("https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/People/mechaninc16.jpg");
            background-size: cover;
            background-position: right center;
            background-repeat: no-repeat;
            /* makes a parallax scroll effect */
            background-attachment: fixed;

            img {
                display: none;
            }
        }
    }
}
                                
/*-- -------------------------- -->
<---          Contact           -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
    :root {
        /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
        --primary: #D90429;
        --primaryLight: #d8673d;
        --secondary: #FFBA43;
        --secondaryLight: #FFBA43;
        --headerColor: #1a1a1a;
        --bodyTextColor: #4E4B66;
		--bodyTextColorWhite: #FAFBFC;

        /* 13px - 16px */
        --topperFontSize: clamp(.8125rem, 1.6vw, 1rem);
        /* 31px - 49px */
        --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
        --bodyFontSize: 1rem;

        /* 60px - 100px top and bottom */
        --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
    }

    #contact-1092 {
        /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
        font-family: 'Roboto', 'Arial', sans-serif;
        background-color: #f7f7f7;
        position: relative;
        z-index: 1;
        
        .cs-container {
            width: 100%;
            /* chnaged to 1280px at large desktop */
            max-width: (1550/16rem);
            margin: auto;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }

        .cs-content {
            /* set text aling to left if content needs to be left aligned */
            text-align: left;
            width: 100%;
            max-width: (784/16rem);
            padding: var(--sectionPadding);
            /* prevents padding from affecting height and width */
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            /* centers content horizontally, set to flex-start to left align */
            align-items: flex-start;
            /* prevents flexbox from squishing it */
            flex: none;
        }
    
        .cs-topper {
            font-size: var(--topperFontSize);
            line-height: 1.2em;
            text-transform: uppercase;
            text-align: inherit;
            letter-spacing: .1em;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: (4/16rem);
            display: block;
        }
    
        .cs-title {
            font-size: var(--headerFontSize);
            font-weight: 900;
            line-height: 1.2em;
            text-align: inherit;
            max-width: (700/16rem);
            margin: 0 0 (16/16rem) 0;
            color: var(--headerColor);
            position: relative;
        }
    
        .cs-text {
            font-size: var(--bodyFontSize);
            line-height: 1.5em;
            text-align: inherit;
            width: 100%;
            max-width: (650/16rem);
            margin: 0;
            color: var(--bodyTextColor);
        }

        .cs-topper {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            color: #767676;
        }

        .cs-title {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            max-width: 20ch;
        }

        .cs-text {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            margin-bottom: (16/16rem);
            &:last-of-type {
                margin-bottom: (32/16rem);
            }
        }

        .cs-form-group {
            width: 100%;
            /* moved section padding to the content so the picture can be full width */
            padding: var(--sectionPadding);
            /* prevents padding from affecting height and width */
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: (48/16rem);
            position: relative;
        }

        .cs-form-content {
            /* set text aling to left if content needs to be left aligned */
            text-align: left;
            width: 100%;
            display: flex;
            flex-direction: column;
            /* centers content horizontally, set to flex-start to left align */
            align-items: flex-start;

            .cs-topper {
                color: var(--secondary);
            }

            .cs-title {
                color: var(--bodyTextColorWhite);
                /* 36px - 48px */
                margin-bottom: clamp(2.25rem, 4vw, 3rem);
            }
        }

        .cs-form {
            width: 100%;
            max-width: (784/16rem);
            /* 32px - 48px top & bottom */
            /* 16px - 48px left & right */
            padding: clamp(2rem, 4vw, 3rem) clamp(1rem, 4vw, 3rem);
            /* prevents padding from affecting height and width */
            box-sizing: border-box;
            background-color: rgba(26, 26, 26, .72);
            display: flex;
            justify-content: space-between;
            flex-wrap: wrap;
            align-items: center;
            gap: (12/16rem);
        }

        .cs-label {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.5vw, 1rem);
            width: 100%;
            color: var(--bodyTextColorWhite);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: flex-start;
            gap: (4/16rem);
        }

        .cs-input {
            font-size: (16/16rem);
            width: 100%;
            height: (56/16rem);
            padding: 0;
            padding-left: (24/16rem);
            color: var(--bodyTextColorWhite);
            background-color: #595756;
            border: none;
            /* prevents padding from adding to height and width */
            box-sizing: border-box;
            transition: border-color .3s;
            &:hover {
                border-color: var(--secondary);
            }
            &:focus {
                outline: 1px solid var(--secondary);
            }
            &::placeholder {
                color: #BABABA;
                opacity: .6;
            }
        }

        .cs-textarea {
            min-height: (120/16rem);
            padding-top: (24/16rem);
            margin: 0;
            font-family: inherit;
        }

        .cs-button-solid {
            font-size: (16/16rem);
            /* 46px - 56px */
            line-height: clamp(2.875rem, 5.5vw, 3.5rem);
            text-decoration: none;
	    	font-weight: 700;
	        text-align: center;
            margin: 0;
            color: #fff;
            min-width: (150/16rem);
            padding: 0 (24/16rem);
            background-color: var(--primary);
            border-radius: (4/16rem);
            display: inline-block;
            position: relative;
            z-index: 1;
            /* prevents padding from adding to the width */
            box-sizing: border-box;

            &:before {
                content: '';
                position: absolute;
                height: 100%;
                width: 0%;
                background: #000;
                opacity: 1;
                top: 0;
                left: 0;
                z-index: -1;
                transition: width .3s;
            }

            &:hover {
                &:before {
                    width: 100%;
                }
            }
        }

        .cs-submit {
            min-width: (241/16rem);
            /* 36px - 48px */
            margin-top: clamp(2.25rem, 4vw, 3rem);
            border: none;
            border-radius: 0;

            &:hover {
                cursor: pointer;
            }
        }

        .cs-background {
            width: 100%;
            height: 100%;
            display: block;
            position: absolute;
            top: 0;
            left: 0;
            z-index: -1;

            &:before {
                /* background color overlay */
                content: '';
                position: absolute;
                display: block;
                height: 100%;
                width: 100%;
                background: #1a1a1a;
                opacity: .72;
                top: 0;
                left: 0;
                z-index: 1;
            }

            img {
                position: absolute;
                top: 0;
                left: 0;
                height: 100%;
                width: 100%;
                /* Makes img tag act as a background image */
                object-fit: cover;
            }
        }

        .cs-icon {
            /* 48px - 72px */
            width: clamp(3rem, 6vw, 4.5rem);
            height: auto;
            /* 28px - 40px */
            margin: 0 0 clamp(1.75rem, 4vw, 2.5rem);
            display: block;
        }

        .cs-review-group {
            /* 24px - 32px top & bottom */
            /* 16px - 32px left & right */
            padding: clamp(1.5rem, 4vw, 2rem) clamp(1rem, 5vw, 2rem);
            /* prevents padding from affecting height and width */
            box-sizing: border-box;
            background-color: #fff;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            // /* 28px 40px *
            gap: clamp(1.75rem, 4vw, 2.5rem);
        }

        .cs-picture {
            /* dimensions change at tablet */
            width: 100%;
            height: (240/16rem);
            /* prevents flexbox from squishing it */
            flex: none;

            img {
                width: 100%;
                max-width: (300/16rem);
                height: 100%;
                object-fit: cover;
                /* places the top of the image to the top of the parent */
                object-position: top;
            }
        }

        .cs-review-text {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.5vw, 1rem);
            line-height: 1.5em;
            text-align: left;
            /* 16px - 28px */
            margin: 0 0 clamp(1rem, 3vw, 1.75rem);
            color: var(--bodyTextColor);
        }

        .cs-name {
            /* 20px - 25px */
            font-size: clamp(1.25rem, 3vw, 1.5625rem);
            line-height: 1.2em;
            font-weight: bold;
            text-align: left;
            margin: 0;
            display: block;
        }

        .cs-desc {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.5vw, 1rem);
            line-height: 1.5em;
            text-align: left;
            margin: 0;
            color: var(--bodyTextColor);
            display: block;
        }
    }
}

/* Tablet - 768px */
@media only screen and (min-width: 48rem) {
    #contact-1092 {
        .cs-review-group {
            flex-direction: row;
            justify-content: space-between;
        }

        .cs-picture {
            width: (179/16rem);
            height: (202/16rem);
        }
    }
}

/* Large Desktop - 1500px */
@media only screen and (min-width: 93.75rem) {
    #contact-1092 {
        padding: var(--sectionPadding);
        position: relative;

        .cs-container {
            max-width: (1280/16rem);
            flex-direction: row;
            justify-content: space-between;
            gap: (90/16rem);
        }

        .cs-content {
            max-width: (630/16rem);
            padding: 0;
        }

        .cs-form-group {
            padding: 0;
            /* undo the position, so the cs-background will position itself relative to the next parent that has a decalred position relative, which we added to the #content parent div. Now we can position it relative to the whole section, allowing us to have more control on how it covers the section. */
            position: initial;
        }

        .cs-form {
            transform: translateX(-80/16rem);
        }

        .cs-background {
            width: 50vw;
            margin-right: (80/16rem);
            right: 50%;
            left: auto;
            background: url("https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/People/mechaninc16.jpg");
            background-size: cover;
            background-position: right center;
            background-repeat: no-repeat;
            /* makes a parallax scroll effect */
            background-attachment: fixed;

            img {
                display: none;
            }
        }
    }
}

/* Dark Mode */
@media only screen and (min-width: 0rem) {
    body.dark-mode {
        #contact-1092 {
            background-color: rgba(0, 0, 0, .6);

            .cs-topper {
                color: var(--secondary);
            }

            .cs-title, .cs-text, .cs-label, .cs-link, .cs-review-text, .cs-name, .cs-desc {
                color: var(--bodyTextColorWhite);
            }

            .cs-text, .cs-desc {
                opacity: .8;
            }

            .cs-icon {
                opacity: .5;
            }

            .cs-form {
                background-color: rgba(0, 0, 0, .84);
            }

            .cs-review-group {
                background-color: var(--medium);
            }

            .cs-button-solid {
                transition: color .3s;

                &:before {
                    background-color: #fff;
                }

                &:hover {
                    color: var(--primary);
                }
            }

            .cs-background {
                &:before {
                    opacity: .90;
                    background-color: #000;
                }
            }
        }
    }
}
                                
/*-- -------------------------- -->
<---          Contact           -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
    :root {
        /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
        --primary: #D90429;
        --primaryLight: #d8673d;
        --secondary: #FFBA43;
        --secondaryLight: #FFBA43;
        --headerColor: #1a1a1a;
        --bodyTextColor: #4E4B66;
		--bodyTextColorWhite: #FAFBFC;

        /* 13px - 16px */
        --topperFontSize: clamp(.8125rem, 1.6vw, 1rem);
        /* 31px - 49px */
        --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
        --bodyFontSize: 1rem;

        /* 60px - 100px top and bottom */
        --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
    }

    #contact-1092 {
        /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
        font-family: 'Roboto', 'Arial', sans-serif;
        background-color: #f7f7f7;
        position: relative;
        z-index: 1;
        
        .cs-container {
            width: 100%;
            /* chnaged to 1280px at large desktop */
            max-width: calc(1550 / 16 * 1rem);
            margin: auto;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }

        .cs-content {
            /* set text aling to left if content needs to be left aligned */
            text-align: left;
            width: 100%;
            max-width: calc(784 / 16 * 1rem);
            padding: var(--sectionPadding);
            /* prevents padding from affecting height and width */
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            /* centers content horizontally, set to flex-start to left align */
            align-items: flex-start;
            /* prevents flexbox from squishing it */
            flex: none;
        }
    
        .cs-topper {
            font-size: var(--topperFontSize);
            line-height: 1.2em;
            text-transform: uppercase;
            text-align: inherit;
            letter-spacing: .1em;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: calc(4 / 16 * 1rem);
            display: block;
        }
    
        .cs-title {
            font-size: var(--headerFontSize);
            font-weight: 900;
            line-height: 1.2em;
            text-align: inherit;
            max-width: calc(700 / 16 * 1rem);
            margin: 0 0 calc(16 / 16 * 1rem) 0;
            color: var(--headerColor);
            position: relative;
        }
    
        .cs-text {
            font-size: var(--bodyFontSize);
            line-height: 1.5em;
            text-align: inherit;
            width: 100%;
            max-width: calc(650 / 16 * 1rem);
            margin: 0;
            color: var(--bodyTextColor);
        }

        .cs-topper {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            color: #767676;
        }

        .cs-title {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            max-width: 20ch;
        }

        .cs-text {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            margin-bottom: calc(16 / 16 * 1rem);
            &:last-of-type {
                margin-bottom: calc(32 / 16 * 1rem);
            }
        }

        .cs-form-group {
            width: 100%;
            /* moved section padding to the content so the picture can be full width */
            padding: var(--sectionPadding);
            /* prevents padding from affecting height and width */
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: calc(48 / 16 * 1rem);
            position: relative;
        }

        .cs-form-content {
            /* set text aling to left if content needs to be left aligned */
            text-align: left;
            width: 100%;
            display: flex;
            flex-direction: column;
            /* centers content horizontally, set to flex-start to left align */
            align-items: flex-start;

            .cs-topper {
                color: var(--secondary);
            }

            .cs-title {
                color: var(--bodyTextColorWhite);
                /* 36px - 48px */
                margin-bottom: clamp(2.25rem, 4vw, 3rem);
            }
        }

        .cs-form {
            width: 100%;
            max-width: calc(784 / 16 * 1rem);
            /* 32px - 48px top & bottom */
            /* 16px - 48px left & right */
            padding: clamp(2rem, 4vw, 3rem) clamp(1rem, 4vw, 3rem);
            /* prevents padding from affecting height and width */
            box-sizing: border-box;
            background-color: rgba(26, 26, 26, .72);
            display: flex;
            justify-content: space-between;
            flex-wrap: wrap;
            align-items: center;
            gap: calc(12 / 16 * 1rem);
        }

        .cs-label {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.5vw, 1rem);
            width: 100%;
            color: var(--bodyTextColorWhite);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: flex-start;
            gap: calc(4 / 16 * 1rem);
        }

        .cs-input {
            font-size: calc(16 / 16 * 1rem);
            width: 100%;
            height: calc(56 / 16 * 1rem);
            padding: 0;
            padding-left: calc(24 / 16 * 1rem);
            color: var(--bodyTextColorWhite);
            background-color: #595756;
            border: none;
            /* prevents padding from adding to height and width */
            box-sizing: border-box;
            transition: border-color .3s;
            &:hover {
                border-color: var(--secondary);
            }
            &:focus {
                outline: 1px solid var(--secondary);
            }
            &::placeholder {
                color: #BABABA;
                opacity: .6;
            }
        }

        .cs-textarea {
            min-height: calc(120 / 16 * 1rem);
            padding-top: calc(24 / 16 * 1rem);
            margin: 0;
            font-family: inherit;
        }

        .cs-button-solid {
            font-size: calc(16 / 16 * 1rem);
            /* 46px - 56px */
            line-height: clamp(2.875rem, 5.5vw, 3.5rem);
            text-decoration: none;
	    	font-weight: 700;
	        text-align: center;
            margin: 0;
            color: #fff;
            min-width: calc(150 / 16 * 1rem);
            padding: 0 calc(24 / 16 * 1rem);
            background-color: var(--primary);
            border-radius: calc(4 / 16 * 1rem);
            display: inline-block;
            position: relative;
            z-index: 1;
            /* prevents padding from adding to the width */
            box-sizing: border-box;

            &:before {
                content: '';
                position: absolute;
                height: 100%;
                width: 0%;
                background: #000;
                opacity: 1;
                top: 0;
                left: 0;
                z-index: -1;
                transition: width .3s;
            }

            &:hover {
                &:before {
                    width: 100%;
                }
            }
        }

        .cs-submit {
            min-width: calc(241 / 16 * 1rem);
            /* 36px - 48px */
            margin-top: clamp(2.25rem, 4vw, 3rem);
            border: none;
            border-radius: 0;

            &:hover {
                cursor: pointer;
            }
        }

        .cs-background {
            width: 100%;
            height: 100%;
            display: block;
            position: absolute;
            top: 0;
            left: 0;
            z-index: -1;

            &:before {
                /* background color overlay */
                content: '';
                position: absolute;
                display: block;
                height: 100%;
                width: 100%;
                background: #1a1a1a;
                opacity: .72;
                top: 0;
                left: 0;
                z-index: 1;
            }

            img {
                position: absolute;
                top: 0;
                left: 0;
                height: 100%;
                width: 100%;
                /* Makes img tag act as a background image */
                object-fit: cover;
            }
        }

        .cs-icon {
            /* 48px - 72px */
            width: clamp(3rem, 6vw, 4.5rem);
            height: auto;
            /* 28px - 40px */
            margin: 0 0 clamp(1.75rem, 4vw, 2.5rem);
            display: block;
        }

        .cs-review-group {
            /* 24px - 32px top & bottom */
            /* 16px - 32px left & right */
            padding: clamp(1.5rem, 4vw, 2rem) clamp(1rem, 5vw, 2rem);
            /* prevents padding from affecting height and width */
            box-sizing: border-box;
            background-color: #fff;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            // /* 28px 40px *
            gap: clamp(1.75rem, 4vw, 2.5rem);
        }

        .cs-picture {
            /* dimensions change at tablet */
            width: 100%;
            height: calc(240 / 16 * 1rem);
            /* prevents flexbox from squishing it */
            flex: none;

            img {
                width: 100%;
                max-width: calc(300 / 16 * 1rem);
                height: 100%;
                object-fit: cover;
                /* places the top of the image to the top of the parent */
                object-position: top;
            }
        }

        .cs-review-text {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.5vw, 1rem);
            line-height: 1.5em;
            text-align: left;
            /* 16px - 28px */
            margin: 0 0 clamp(1rem, 3vw, 1.75rem);
            color: var(--bodyTextColor);
        }

        .cs-name {
            /* 20px - 25px */
            font-size: clamp(1.25rem, 3vw, 1.5625rem);
            line-height: 1.2em;
            font-weight: bold;
            text-align: left;
            margin: 0;
            display: block;
        }

        .cs-desc {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.5vw, 1rem);
            line-height: 1.5em;
            text-align: left;
            margin: 0;
            color: var(--bodyTextColor);
            display: block;
        }
    }
}

/* Tablet - 768px */
@media only screen and (min-width: 48rem) {
    #contact-1092 {
        .cs-review-group {
            flex-direction: row;
            justify-content: space-between;
        }

        .cs-picture {
            width: calc(179 / 16 * 1rem);
            height: calc(202 / 16 * 1rem);
        }
    }
}

/* Large Desktop - 1500px */
@media only screen and (min-width: 93.75rem) {
    #contact-1092 {
        padding: var(--sectionPadding);
        position: relative;

        .cs-container {
            max-width: calc(1280 / 16 * 1rem);
            flex-direction: row;
            justify-content: space-between;
            gap: calc(90 / 16 * 1rem);
        }

        .cs-content {
            max-width: calc(630 / 16 * 1rem);
            padding: 0;
        }

        .cs-form-group {
            padding: 0;
            /* undo the position, so the cs-background will position itself relative to the next parent that has a decalred position relative, which we added to the #content parent div. Now we can position it relative to the whole section, allowing us to have more control on how it covers the section. */
            position: initial;
        }

        .cs-form {
            transform: translateX(-5rem);
        }

        .cs-background {
            width: 50vw;
            margin-right: calc(80 / 16 * 1rem);
            right: 50%;
            left: auto;
            background: url("https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/People/mechaninc16.jpg");
            background-size: cover;
            background-position: right center;
            background-repeat: no-repeat;
            /* makes a parallax scroll effect */
            background-attachment: fixed;

            img {
                display: none;
            }
        }
    }
}
                                
/*-- -------------------------- -->
<---          Contact           -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
    :root {
        /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
        --primary: #D90429;
        --primaryLight: #d8673d;
        --secondary: #FFBA43;
        --secondaryLight: #FFBA43;
        --headerColor: #1a1a1a;
        --bodyTextColor: #4E4B66;
		--bodyTextColorWhite: #FAFBFC;

        /* 13px - 16px */
        --topperFontSize: clamp(.8125rem, 1.6vw, 1rem);
        /* 31px - 49px */
        --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
        --bodyFontSize: 1rem;

        /* 60px - 100px top and bottom */
        --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
    }

    #contact-1092 {
        /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
        font-family: 'Roboto', 'Arial', sans-serif;
        background-color: #f7f7f7;
        position: relative;
        z-index: 1;
        
        .cs-container {
            width: 100%;
            /* chnaged to 1280px at large desktop */
            max-width: calc(1550 / 16 * 1rem);
            margin: auto;
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }

        .cs-content {
            /* set text aling to left if content needs to be left aligned */
            text-align: left;
            width: 100%;
            max-width: calc(784 / 16 * 1rem);
            padding: var(--sectionPadding);
            /* prevents padding from affecting height and width */
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            /* centers content horizontally, set to flex-start to left align */
            align-items: flex-start;
            /* prevents flexbox from squishing it */
            flex: none;
        }
    
        .cs-topper {
            font-size: var(--topperFontSize);
            line-height: 1.2em;
            text-transform: uppercase;
            text-align: inherit;
            letter-spacing: .1em;
            font-weight: 700;
            color: var(--primary);
            margin-bottom: calc(4 / 16 * 1rem);
            display: block;
        }
    
        .cs-title {
            font-size: var(--headerFontSize);
            font-weight: 900;
            line-height: 1.2em;
            text-align: inherit;
            max-width: calc(700 / 16 * 1rem);
            margin: 0 0 calc(16 / 16 * 1rem) 0;
            color: var(--headerColor);
            position: relative;
        }
    
        .cs-text {
            font-size: var(--bodyFontSize);
            line-height: 1.5em;
            text-align: inherit;
            width: 100%;
            max-width: calc(650 / 16 * 1rem);
            margin: 0;
            color: var(--bodyTextColor);
        }

        .cs-topper {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            color: #767676;
        }

        .cs-title {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            max-width: 20ch;
        }

        .cs-text {
            /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
            margin-bottom: calc(16 / 16 * 1rem);
            &:last-of-type {
                margin-bottom: calc(32 / 16 * 1rem);
            }
        }

        .cs-form-group {
            width: 100%;
            /* moved section padding to the content so the picture can be full width */
            padding: var(--sectionPadding);
            /* prevents padding from affecting height and width */
            box-sizing: border-box;
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: calc(48 / 16 * 1rem);
            position: relative;
        }

        .cs-form-content {
            /* set text aling to left if content needs to be left aligned */
            text-align: left;
            width: 100%;
            display: flex;
            flex-direction: column;
            /* centers content horizontally, set to flex-start to left align */
            align-items: flex-start;

            .cs-topper {
                color: var(--secondary);
            }

            .cs-title {
                color: var(--bodyTextColorWhite);
                /* 36px - 48px */
                margin-bottom: clamp(2.25rem, 4vw, 3rem);
            }
        }

        .cs-form {
            width: 100%;
            max-width: calc(784 / 16 * 1rem);
            /* 32px - 48px top & bottom */
            /* 16px - 48px left & right */
            padding: clamp(2rem, 4vw, 3rem) clamp(1rem, 4vw, 3rem);
            /* prevents padding from affecting height and width */
            box-sizing: border-box;
            background-color: rgba(26, 26, 26, .72);
            display: flex;
            justify-content: space-between;
            flex-wrap: wrap;
            align-items: center;
            gap: calc(12 / 16 * 1rem);
        }

        .cs-label {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.5vw, 1rem);
            width: 100%;
            color: var(--bodyTextColorWhite);
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: flex-start;
            gap: calc(4 / 16 * 1rem);
        }

        .cs-input {
            font-size: calc(16 / 16 * 1rem);
            width: 100%;
            height: calc(56 / 16 * 1rem);
            padding: 0;
            padding-left: calc(24 / 16 * 1rem);
            color: var(--bodyTextColorWhite);
            background-color: #595756;
            border: none;
            /* prevents padding from adding to height and width */
            box-sizing: border-box;
            transition: border-color .3s;
            &:hover {
                border-color: var(--secondary);
            }
            &:focus {
                outline: 1px solid var(--secondary);
            }
            &::placeholder {
                color: #BABABA;
                opacity: .6;
            }
        }

        .cs-textarea {
            min-height: calc(120 / 16 * 1rem);
            padding-top: calc(24 / 16 * 1rem);
            margin: 0;
            font-family: inherit;
        }

        .cs-button-solid {
            font-size: calc(16 / 16 * 1rem);
            /* 46px - 56px */
            line-height: clamp(2.875rem, 5.5vw, 3.5rem);
            text-decoration: none;
	    	font-weight: 700;
	        text-align: center;
            margin: 0;
            color: #fff;
            min-width: calc(150 / 16 * 1rem);
            padding: 0 calc(24 / 16 * 1rem);
            background-color: var(--primary);
            border-radius: calc(4 / 16 * 1rem);
            display: inline-block;
            position: relative;
            z-index: 1;
            /* prevents padding from adding to the width */
            box-sizing: border-box;

            &:before {
                content: '';
                position: absolute;
                height: 100%;
                width: 0%;
                background: #000;
                opacity: 1;
                top: 0;
                left: 0;
                z-index: -1;
                transition: width .3s;
            }

            &:hover {
                &:before {
                    width: 100%;
                }
            }
        }

        .cs-submit {
            min-width: calc(241 / 16 * 1rem);
            /* 36px - 48px */
            margin-top: clamp(2.25rem, 4vw, 3rem);
            border: none;
            border-radius: 0;

            &:hover {
                cursor: pointer;
            }
        }

        .cs-background {
            width: 100%;
            height: 100%;
            display: block;
            position: absolute;
            top: 0;
            left: 0;
            z-index: -1;

            &:before {
                /* background color overlay */
                content: '';
                position: absolute;
                display: block;
                height: 100%;
                width: 100%;
                background: #1a1a1a;
                opacity: .72;
                top: 0;
                left: 0;
                z-index: 1;
            }

            img {
                position: absolute;
                top: 0;
                left: 0;
                height: 100%;
                width: 100%;
                /* Makes img tag act as a background image */
                object-fit: cover;
            }
        }

        .cs-icon {
            /* 48px - 72px */
            width: clamp(3rem, 6vw, 4.5rem);
            height: auto;
            /* 28px - 40px */
            margin: 0 0 clamp(1.75rem, 4vw, 2.5rem);
            display: block;
        }

        .cs-review-group {
            /* 24px - 32px top & bottom */
            /* 16px - 32px left & right */
            padding: clamp(1.5rem, 4vw, 2rem) clamp(1rem, 5vw, 2rem);
            /* prevents padding from affecting height and width */
            box-sizing: border-box;
            background-color: #fff;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            // /* 28px 40px *
            gap: clamp(1.75rem, 4vw, 2.5rem);
        }

        .cs-picture {
            /* dimensions change at tablet */
            width: 100%;
            height: calc(240 / 16 * 1rem);
            /* prevents flexbox from squishing it */
            flex: none;

            img {
                width: 100%;
                max-width: calc(300 / 16 * 1rem);
                height: 100%;
                object-fit: cover;
                /* places the top of the image to the top of the parent */
                object-position: top;
            }
        }

        .cs-review-text {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.5vw, 1rem);
            line-height: 1.5em;
            text-align: left;
            /* 16px - 28px */
            margin: 0 0 clamp(1rem, 3vw, 1.75rem);
            color: var(--bodyTextColor);
        }

        .cs-name {
            /* 20px - 25px */
            font-size: clamp(1.25rem, 3vw, 1.5625rem);
            line-height: 1.2em;
            font-weight: bold;
            text-align: left;
            margin: 0;
            display: block;
        }

        .cs-desc {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.5vw, 1rem);
            line-height: 1.5em;
            text-align: left;
            margin: 0;
            color: var(--bodyTextColor);
            display: block;
        }
    }
}

/* Tablet - 768px */
@media only screen and (min-width: 48rem) {
    #contact-1092 {
        .cs-review-group {
            flex-direction: row;
            justify-content: space-between;
        }

        .cs-picture {
            width: calc(179 / 16 * 1rem);
            height: calc(202 / 16 * 1rem);
        }
    }
}

/* Large Desktop - 1500px */
@media only screen and (min-width: 93.75rem) {
    #contact-1092 {
        padding: var(--sectionPadding);
        position: relative;

        .cs-container {
            max-width: calc(1280 / 16 * 1rem);
            flex-direction: row;
            justify-content: space-between;
            gap: calc(90 / 16 * 1rem);
        }

        .cs-content {
            max-width: calc(630 / 16 * 1rem);
            padding: 0;
        }

        .cs-form-group {
            padding: 0;
            /* undo the position, so the cs-background will position itself relative to the next parent that has a decalred position relative, which we added to the #content parent div. Now we can position it relative to the whole section, allowing us to have more control on how it covers the section. */
            position: initial;
        }

        .cs-form {
            transform: translateX(-5rem);
        }

        .cs-background {
            width: 50vw;
            margin-right: calc(80 / 16 * 1rem);
            right: 50%;
            left: auto;
            background: url("https://csimg.nyc3.cdn.digitaloceanspaces.com/Images/People/mechaninc16.jpg");
            background-size: cover;
            background-position: right center;
            background-repeat: no-repeat;
            /* makes a parallax scroll effect */
            background-attachment: fixed;

            img {
                display: none;
            }
        }
    }
}

/* Dark Mode */
@media only screen and (min-width: 0rem) {
    body.dark-mode {
        #contact-1092 {
            background-color: rgba(0, 0, 0, .6);

            .cs-topper {
                color: var(--secondary);
            }

            .cs-title, .cs-text, .cs-label, .cs-link, .cs-review-text, .cs-name, .cs-desc {
                color: var(--bodyTextColorWhite);
            }

            .cs-text, .cs-desc {
                opacity: .8;
            }

            .cs-icon {
                opacity: .5;
            }

            .cs-form {
                background-color: rgba(0, 0, 0, .84);
            }

            .cs-review-group {
                background-color: var(--medium);
            }

            .cs-button-solid {
                transition: color .3s;

                &:before {
                    background-color: #fff;
                }

                &:hover {
                    color: var(--primary);
                }
            }

            .cs-background {
                &:before {
                    opacity: .90;
                    background-color: #000;
                }
            }
        }
    }
}
                                