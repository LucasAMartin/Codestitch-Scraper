<!--                   
    This is a cs-content section that you can swap out with any cs-content you have in your site that is not a Side By Side. 
-->

<div id="cs-content-829">
    <div class="cs-flex">
        <span class="cs-topper">About Us</span>
        <h2 class="cs-title">Laborum magna nulla  Consecte duis</h2>
    </div>
    <p class="cs-text">
        Lorem ipsum dolor sit amet consectetur adipisicing elit. Quam tempora nulla sit reprehenderit quod suscipit, soluta minus impedit libero dolor et aspernatur ratione blanditiis nihil!
    </p>
</div>
                                
/*-- -------------------------- -->
<---         CS-Content         -->
<--- -------------------------- -*/

/* you can place this CSS group in your global stylesheet that is loaded on every page of the site so all you need to do is copy and paste the HTML into any section and it will all work. Or piece it inside the media queries in whatever section you want to add it to. You can also remove the cs-topper, cs-title, and cs-text and if they aren't already in your global stylesheet you can paste them outside these media queries so all cs-topper, cs-title, and cs-text on your site will pull from 1 place. */

/* Mobile */
@media only screen and (min-width: 0rem) {
  #cs-content-829 {
    /* set text align to left if content needs to be left aligned */
    text-align: left;
    width: 100%;
    display: flex;
    flex-direction: column;
    /* centers content horizontally, set to flex-start to left align */
    align-items: flex-start;
  }
  #cs-content-829 .cs-topper {
    font-size: var(--topperFontSize);
    line-height: 1.2em;
    text-transform: uppercase;
    text-align: inherit;
    letter-spacing: .1em;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 0.25rem;
    display: block;
  }
  #cs-content-829 .cs-title {
    font-size: var(--headerFontSize);
    font-weight: 900;
    line-height: 1.2em;
    text-align: inherit;
    max-width: 43.75rem;
    margin: 0 0 1rem 0;
    color: var(--headerColor);
    position: relative;
  }
  #cs-content-829 .cs-text {
    font-size: var(--bodyFontSize);
    line-height: 1.5em;
    text-align: inherit;
    width: 100%;
    max-width: 40.625rem;
    margin: 0;
    color: var(--bodyTextColor);
  }
  #cs-content-829 .cs-title {
    /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
    max-width: 20ch;
  }
  #cs-content-829 .cs-text {
    /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
    max-width: 31.5rem;
  }
  #cs-content-829 .cs-text:before {
    /* divider line */
    content: '';
    width: 7.8125rem;
    height: 3px;
    margin-bottom: 1rem;
    background: var(--primary);
    opacity: 1;
    display: block;
    position: relative;
  }
}
/* Tablet - 768px */
@media only screen and (min-width: 48rem) {
  #cs-content-829 {
    flex-direction: row;
    justify-content: center;
    align-items: center;
  }
  #cs-content-829 .cs-flex {
    width: 50%;
    max-width: 34.375rem;
    flex: none;
  }
  #cs-content-829 .cs-topper,
  #cs-content-829 .cs-title {
    text-align: right;
  }
  #cs-content-829 .cs-title {
    margin: 0;
  }
  #cs-content-829 .cs-text {
    display: flex;
    justify-content: center;
    align-items: center;
  }
  #cs-content-829 .cs-text:before {
    width: 3px;
    height: 7.8125rem;
    /* 24px - 28px */
    margin: 0 clamp(1.5rem, 3vw, 1.75rem);
    /* prevents flexbox from squishing it */
    flex: none;
  }
}
                                
/*-- -------------------------- -->
<---         CS-Content         -->
<--- -------------------------- -*/

/* you can place this CSS group in your global stylesheet that is loaded on every page of the site so all you need to do is copy and paste the HTML into any section and it will all work. Or piece it inside the media queries in whatever section you want to add it to. You can also remove the cs-topper, cs-title, and cs-text and if they aren't already in your global stylesheet you can paste them outside these media queries so all cs-topper, cs-title, and cs-text on your site will pull from 1 place. */

/* Mobile */
@media only screen and (min-width: 0rem) {
  #cs-content-829 {
    /* set text align to left if content needs to be left aligned */
    text-align: left;
    width: 100%;
    display: flex;
    flex-direction: column;
    /* centers content horizontally, set to flex-start to left align */
    align-items: flex-start;
  }
  #cs-content-829 .cs-topper {
    font-size: var(--topperFontSize);
    line-height: 1.2em;
    text-transform: uppercase;
    text-align: inherit;
    letter-spacing: .1em;
    font-weight: 700;
    color: var(--primary);
    margin-bottom: 0.25rem;
    display: block;
  }
  #cs-content-829 .cs-title {
    font-size: var(--headerFontSize);
    font-weight: 900;
    line-height: 1.2em;
    text-align: inherit;
    max-width: 43.75rem;
    margin: 0 0 1rem 0;
    color: var(--headerColor);
    position: relative;
  }
  #cs-content-829 .cs-text {
    font-size: var(--bodyFontSize);
    line-height: 1.5em;
    text-align: inherit;
    width: 100%;
    max-width: 40.625rem;
    margin: 0;
    color: var(--bodyTextColor);
  }
  #cs-content-829 .cs-title {
    /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
    max-width: 20ch;
  }
  #cs-content-829 .cs-text {
    /* Override.  cs-topper cs-title and first cs-text should be removed and put into your global css sheet so it can control every instance of them on your site and is consistent. This selector is a section specific override that stays inside this stitch */
    max-width: 31.5rem;
  }
  #cs-content-829 .cs-text:before {
    /* divider line */
    content: '';
    width: 7.8125rem;
    height: 3px;
    margin-bottom: 1rem;
    background: var(--primary);
    opacity: 1;
    display: block;
    position: relative;
  }
}
/* Tablet - 768px */
@media only screen and (min-width: 48rem) {
  #cs-content-829 {
    flex-direction: row;
    justify-content: center;
    align-items: center;
  }
  #cs-content-829 .cs-flex {
    width: 50%;
    max-width: 34.375rem;
    flex: none;
  }
  #cs-content-829 .cs-topper,
  #cs-content-829 .cs-title {
    text-align: right;
  }
  #cs-content-829 .cs-title {
    margin: 0;
  }
  #cs-content-829 .cs-text {
    display: flex;
    justify-content: center;
    align-items: center;
  }
  #cs-content-829 .cs-text:before {
    width: 3px;
    height: 7.8125rem;
    /* 24px - 28px */
    margin: 0 clamp(1.5rem, 3vw, 1.75rem);
    /* prevents flexbox from squishing it */
    flex: none;
  }
}
/* Dark Mode */
@media only screen and (min-width: 0rem) {
  body.dark-mode #cs-content-829 .cs-title,
  body.dark-mode #cs-content-829 .cs-text,
  body.dark-mode #cs-content-829 .cs-h3,
  body.dark-mode #cs-content-829 .cs-item-text {
    color: var(--bodyTextColorWhite);
  }
}
                                