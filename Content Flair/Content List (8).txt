<!--                   
    These "Micro Stitches" are to be placed inside the .cs-content group of Side By Side Stitches.  Typically after the .cs-title or .cs-text. They are used to add more design flair to a Stitch as well as convey more information in a different way.
-->

<ul id="list-823" class="cs-ul">
    <li class="cs-li">
        <img class="cs-icon" aria-hidden="true" src="https://csimg.nyc3.cdn.digitaloceanspaces.com/SideBySide/sheild.svg" loading="lazy" decoding="async" alt="icon" width="40" height="40">
        Lorem Ipsum Dolor Sit Amet Consectectur
    </li>
    <li class="cs-li">
        <img class="cs-icon" aria-hidden="true" src="https://csimg.nyc3.cdn.digitaloceanspaces.com/SideBySide/pencil.svg" loading="lazy" decoding="async" alt="icon" width="40" height="40">
        Lorem Ipsum Dolor Sit Amet Consectectur
    </li>
    <li class="cs-li">
        <img class="cs-icon" aria-hidden="true" src="https://csimg.nyc3.cdn.digitaloceanspaces.com/SideBySide/hat.svg" loading="lazy" decoding="async" alt="icon" width="40" height="40">
        Lorem Ipsum Dolor Sit Amet Consectectur
    </li>
</ul>
                                
/* Mobile - 360px */
/* you can copy and paste these styles into the section CSS where you placed the #list-823 HTML, or you can place it in your global stylesheet so you can reuse the HTML in any Side By Side section on your site. That way when you make changes to it, it will also change everywhere in the site */
#list-823 {
  width: 80%;
  max-width: 23rem;
  /* 32px - 36px */
  margin: 0 0 clamp(2rem, 4vw, 2.25rem) 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  /* 24px - 32px */
  row-gap: clamp(1.5rem, 4vw, 2rem);
}
#list-823 .cs-li {
  /* 14px - 16px */
  font-size: clamp(0.875rem, 1.5vw, 1rem);
  list-style: none;
  line-height: 1.5em;
  font-weight: 700;
  margin: 0;
  color: var(--headerColor);
  display: flex;
  justify-content: flex-start;
  align-items: center;
  gap: 2rem;
}
                                
/* Mobile - 360px */
/* you can copy and paste these styles into the section CSS where you placed the #list-823 HTML, or you can place it in your global stylesheet so you can reuse the HTML in any Side By Side section on your site. That way when you make changes to it, it will also change everywhere in the site */
#list-823 {
  width: 80%;
  max-width: 23rem;
  /* 32px - 36px */
  margin: 0 0 clamp(2rem, 4vw, 2.25rem) 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  /* 24px - 32px */
  row-gap: clamp(1.5rem, 4vw, 2rem);
}
#list-823 .cs-li {
  /* 14px - 16px */
  font-size: clamp(0.875rem, 1.5vw, 1rem);
  list-style: none;
  line-height: 1.5em;
  font-weight: 700;
  margin: 0;
  color: var(--headerColor);
  display: flex;
  justify-content: flex-start;
  align-items: center;
  gap: 2rem;
}
/* Dark Mode, copy and paste only the styles inside the #list-823 brackets into your section's dark mode media query */
body.dark-mode #list-823 .cs-li {
  color: var(--bodyTextColorWhite);
}
                                
/* Mobile - 360px */
/* you can copy and paste these styles into the section CSS where you placed the #list-823 HTML, or you can place it in your global stylesheet so you can reuse the HTML in any Side By Side section on your site. That way when you make changes to it, it will also change everywhere in the site */
#list-823 {
    width: 80%;
    max-width: (368/16rem);
    /* 32px - 36px */
    margin: 0 0 clamp(2rem, 4vw, 2.25rem) 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    /* 24px - 32px */
    row-gap: clamp(1.5rem, 4vw, 2rem);

    .cs-li {
        /* 14px - 16px */
        font-size: clamp(.875rem, 1.5vw, 1rem);
        list-style: none;
        line-height: 1.5em;
        font-weight: 700;
        margin: 0;
        color: var(--headerColor);
        display: flex;
        justify-content: flex-start;
        align-items: center;
        gap: (32/16rem);
    }
}
                                
/* Mobile - 360px */
/* you can copy and paste these styles into the section CSS where you placed the #list-823 HTML, or you can place it in your global stylesheet so you can reuse the HTML in any Side By Side section on your site. That way when you make changes to it, it will also change everywhere in the site */
#list-823 {
    width: 80%;
    max-width: (368/16rem);
    /* 32px - 36px */
    margin: 0 0 clamp(2rem, 4vw, 2.25rem) 0;
    padding: 0;
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    /* 24px - 32px */
    row-gap: clamp(1.5rem, 4vw, 2rem);

    .cs-li {
        /* 14px - 16px */
        font-size: clamp(.875rem, 1.5vw, 1rem);
        list-style: none;
        line-height: 1.5em;
        font-weight: 700;
        margin: 0;
        color: var(--headerColor);
        display: flex;
        justify-content: flex-start;
        align-items: center;
        gap: (32/16rem);
    }
}

/* Dark Mode, copy and paste only the styles inside the #list-823 brackets into your section's dark mode media query */
body.dark-mode {
    #list-823 {
        .cs-li {
            color: var(--bodyTextColorWhite);
        }
    }
}
                                
/* Mobile - 360px */
/* you can copy and paste these styles into the section CSS where you placed the #list-823 HTML, or you can place it in your global stylesheet so you can reuse the HTML in any Side By Side section on your site. That way when you make changes to it, it will also change everywhere in the site */
#list-823 {
	width: 80%;
	max-width: calc(368 / 16 * 1rem);
	/* 32px - 36px */
	margin: 0 0 clamp(2rem, 4vw, 2.25rem) 0;
	padding: 0;
	display: flex;
	flex-direction: column;
	align-items: flex-start;
	/* 24px - 32px */
	row-gap: clamp(1.5rem, 4vw, 2rem);

	.cs-li {
		/* 14px - 16px */
		font-size: clamp(0.875rem, 1.5vw, 1rem);
		list-style: none;
		line-height: 1.5em;
		font-weight: 700;
		margin: 0;
		color: var(--headerColor);
		display: flex;
		justify-content: flex-start;
		align-items: center;
		gap: calc(32 / 16 * 1rem);
	}
}
                                
/* Mobile - 360px */
/* you can copy and paste these styles into the section CSS where you placed the #list-823 HTML, or you can place it in your global stylesheet so you can reuse the HTML in any Side By Side section on your site. That way when you make changes to it, it will also change everywhere in the site */
#list-823 {
	width: 80%;
	max-width: calc(368 / 16 * 1rem);
	/* 32px - 36px */
	margin: 0 0 clamp(2rem, 4vw, 2.25rem) 0;
	padding: 0;
	display: flex;
	flex-direction: column;
	align-items: flex-start;
	/* 24px - 32px */
	row-gap: clamp(1.5rem, 4vw, 2rem);

	.cs-li {
		/* 14px - 16px */
		font-size: clamp(0.875rem, 1.5vw, 1rem);
		list-style: none;
		line-height: 1.5em;
		font-weight: 700;
		margin: 0;
		color: var(--headerColor);
		display: flex;
		justify-content: flex-start;
		align-items: center;
		gap: calc(32 / 16 * 1rem);
	}
}

/* Dark Mode, copy and paste only the styles inside the #list-823 brackets into your section's dark mode media query */
body.dark-mode {
	#list-823 {
		.cs-li {
			color: var(--bodyTextColorWhite);
		}
	}
}
                                