<!-- ============================================ -->
<!--                   Footer                     -->
<!-- ============================================ -->

<footer id="cs-footer-144">
    <div class="cs-container">
        <!-- Logo Group -->
        <div class="cs-logo-group">
            <a aria-label="go back to home" class="cs-logo" href="">
                <img class="cs-logo-img" loading="lazy" decoding="async" src="https://csimg.nyc3.digitaloceanspaces.com/Footer/logo.svg" alt="logo" width="240" height="32">
            </a>
            <p class="cs-text">
                Lorem ipsum dolor sit amet, elit. Leo consectetur facilisis massa leo non. A, ultricies libero, sit adipiscing ac habitasse sed. Sed hendrerit dignissim sit in dictum ullamcorper. Tincidunt cras vitae egestas id vel duis sed augue.
            </p>
        </div>
        <!-- Links -->
        <ul class="cs-nav">
            <li class="cs-nav-li">
                <span class="cs-header">Sitemap</span>
            </li>
            <li class="cs-nav-li">
                <a class="cs-nav-link" href="">About</a>
            </li>
            <li class="cs-nav-li">
                <a class="cs-nav-link" href="">Services</a>
            </li>
            <li class="cs-nav-li">
                <a class="cs-nav-link" href="">Portfolio</a>
            </li>
            <li class="cs-nav-li">
                <a class="cs-nav-link" href="">Contact</a>
            </li>
        </ul>
        <ul class="cs-nav">
            <li class="cs-nav-li">
                <span class="cs-header">Services</span>
            </li>
            <li class="cs-nav-li">
                <a class="cs-nav-link" href="">About</a>
            </li>
            <li class="cs-nav-li">
                <a class="cs-nav-link" href="">Services</a>
            </li>
            <li class="cs-nav-li">
                <a class="cs-nav-link" href="">Portfolio</a>
            </li>
            <li class="cs-nav-li">
                <a class="cs-nav-link" href="">Contact</a>
            </li>
        </ul>
        <ul class="cs-nav">
            <li class="cs-nav-li">
                <span class="cs-header">Locations</span>
            </li>
            <li class="cs-nav-li">
                <a class="cs-nav-link" href="">About</a>
            </li>
            <li class="cs-nav-li">
                <a class="cs-nav-link" href="">Services</a>
            </li>
            <li class="cs-nav-li">
                <a class="cs-nav-link" href="">Portfolio</a>
            </li>
            <li class="cs-nav-li">
                <a class="cs-nav-link" href="">Contact</a>
            </li>
        </ul>
        <!-- Contact Info -->
        <ul class="cs-contact">
            <li class="cs-contact-li">
                <a class="cs-contact-link" href="tel: 123-456-7890">(123) 456-7890</a>
            </li>
            <li class="cs-contact-li">
                <a class="cs-contact-link" href="mailto:info@codestitch.com">info@codestitch.com</a>
            </li>
            <li class="cs-contact-li">
                <a class="cs-contact-link cs-address" href="" target="_blank" rel="noopener">
                    <span>321 Parkland Drive,</span> Oak Harbor, WA 98101
                </a>
            </li>
        </ul>
        <!-- Bottom Credits and Social -->
        <div class="cs-bottom">
            <span class="cs-copyright">© 2022 Copyright</span>
            <a class="cs-social-link" aria-label="visit google profile" href="" target="_blank" rel="noopener">
                <img class="cs-social-img" aria-hidden="true" loading="lazy" decoding="async" src="https://csimg.nyc3.digitaloceanspaces.com/Social/google.svg" alt="google" width="11" height="11">
            </a>
            <a class="cs-social-link" aria-label="visit facebook profile" href="" target="_blank" rel="noopener">
                <img class="cs-social-img" aria-hidden="true" loading="lazy" decoding="async" src="https://csimg.nyc3.digitaloceanspaces.com/Social/Facebook.svg" alt="facebook" width="6" height="11">
            </a>
            <a class="cs-social-link" aria-label="visit instagram profile" href="" target="_blank" rel="noopener">
                <img class="cs-social-img" aria-hidden="true" loading="lazy" decoding="async" src="https://csimg.nyc3.digitaloceanspaces.com/Social/instagram.svg" alt="instagram" width="11" height="11">
            </a>
        </div>
    </div>
</footer>
                                
/*-- -------------------------- -->
<---          Footer            -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
  :root {
    /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
    --primary: #001F3F;
    --primaryLight: #001F3F;
    --secondary: #FFBA43;
    --secondaryLight: #FFBA43;
    --headerColor: #1a1a1a;
    --bodyTextColor: #4E4B66;
    --bodyTextColorWhite: #FAFBFC;
    /* 13px - 16px */
    --topperFontSize: clamp(0.8125rem, 1.6vw, 1rem);
    /* 31px - 49px */
    --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
    --bodyFontSize: 1rem;
    /* 60px - 100px top and bottom */
    --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
  }
  #cs-footer-144 {
    /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
    font-family: 'Roboto', 'Arial', sans-serif;
    /* 40px - 100px top and bottom */
    padding: clamp(2.5rem, 7.9vw, 6.25rem) 1rem;
    /* Navigation Links */
    /* Contact Links */
    /* Bottom Credits and Social */
  }
  #cs-footer-144 .cs-container {
    width: 100%;
    /* reset on tablet */
    max-width: 34.375rem;
    margin: auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
  }
  #cs-footer-144 .cs-logo-group {
    /* takes up all the space, lets the other ul's wrap below it */
    width: 100%;
  }
  #cs-footer-144 .cs-logo {
    /* 210px - 240px */
    width: clamp(13.125rem, 8vw, 15rem);
    height: auto;
    display: block;
    /* 28px - 44px */
    margin-bottom: clamp(1.75rem, 4.17vw, 2.75rem);
  }
  #cs-footer-144 .cs-text {
    /* 14px - 16px */
    font-size: clamp(0.875rem, 1.7vw, 1rem);
    margin: 0;
    margin-bottom: 2rem;
    max-width: 20.5rem;
    color: var(--bodyTextColor);
  }
  #cs-footer-144 .cs-logo-img {
    width: 100%;
    height: auto;
  }
  #cs-footer-144 .cs-nav {
    padding: 0;
    margin: 0;
  }
  #cs-footer-144 .cs-nav-li {
    list-style: none;
    margin-bottom: 1.25rem;
  }
  #cs-footer-144 .cs-nav-li:last-of-type {
    margin-bottom: 0;
  }
  #cs-footer-144 .cs-header {
    /* 18px - 20px */
    font-size: clamp(1.125rem, 1.5vw, 1.25rem);
    line-height: 1.5em;
    font-weight: 700;
    color: var(--bodyTextColor);
  }
  #cs-footer-144 .cs-nav-link {
    font-size: 1rem;
    text-decoration: none;
    line-height: 1.5em;
    color: var(--bodyTextColor);
    position: relative;
  }
  #cs-footer-144 .cs-nav-link:before {
    /* underline */
    content: '';
    width: 0%;
    height: 0.125rem;
    background: var(--bodyTextColor);
    opacity: 1;
    position: absolute;
    display: block;
    bottom: -0.125rem;
    left: 0;
    transition: width .3s;
  }
  #cs-footer-144 .cs-nav-link:hover:before {
    width: 100%;
  }
  #cs-footer-144 .cs-contact {
    margin: 2.25rem 0 0;
    padding: 1.5rem 0 1.75rem;
    width: 100%;
    border-top: 1px solid #DAD9E3;
    border-bottom: 1px solid #DAD9E3;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }
  #cs-footer-144 .cs-contact-li {
    list-style: none;
    margin-bottom: 0.75rem;
  }
  #cs-footer-144 .cs-contact-li:last-of-type {
    margin-bottom: 0;
  }
  #cs-footer-144 .cs-contact-link {
    /* 14px - 20px */
    font-size: clamp(0.875rem, 1.57vw, 1.25rem);
    text-decoration: none;
    font-weight: 700;
    line-height: 1.5em;
    text-align: center;
    color: var(--bodyTextColor);
    display: inline-block;
  }
  #cs-footer-144 .cs-contact-link:hover {
    text-decoration: underline;
  }
  #cs-footer-144 .cs-address {
    /* 14px - 16px */
    font-size: clamp(0.875rem, 1.57vw, 1rem);
  }
  #cs-footer-144 .cs-bottom {
    /* 20px - 28px */
    padding-top: clamp(1.25rem, 2vw, 1.75rem);
    width: 100%;
    display: flex;
    /* set everyone to position on the left side */
    justify-content: flex-start;
    align-items: center;
    /* These two are Needed to make 3D transforms look 3D on a child */
    perspective: 700px;
    transform-style: preserve-3d;
  }
  #cs-footer-144 .cs-copyright {
    font-size: 0.9375rem;
    text-align: center;
    color: #7D799C;
    display: block;
    /* margin right auto on a flexbox pushes everyone else to the right */
    margin: 0 auto 0 0;
  }
  #cs-footer-144 .cs-social-link {
    width: 1.25rem;
    height: 1.25rem;
    margin-left: 0.75rem;
    background-color: #4E4B66;
    border-radius: 50%;
    display: flex;
    justify-content: center;
    align-items: center;
    position: relative;
    z-index: 1;
    transition: transform 0.3s, background-color 0.3s;
  }
  #cs-footer-144 .cs-social-link:hover {
    background-color: #1a1a1a;
    transform: translateY(-0.1875rem);
  }
  #cs-footer-144 .cs-social-img {
    height: 0.6875rem;
    width: auto;
    display: block;
  }
}
/* Tablet - 768px */
@media only screen and (min-width: 48rem) {
  #cs-footer-144 .cs-container {
    max-width: 80rem;
  }
  #cs-footer-144 .cs-logo-group {
    width: auto;
    /* 224px - 305px */
    width: clamp(14rem, 23vw, 19.0625rem);
  }
  #cs-footer-144 .cs-nav {
    /* 4px - 12px */
    margin-top: clamp(0.25rem, 0.6vw, 0.75rem);
  }
  #cs-footer-144 .cs-contact {
    flex-direction: row;
    justify-content: space-between;
    border-top: none;
  }
  #cs-footer-144 .cs-contact-li {
    margin: 0;
  }
}
/* Small Desktop - 1024px */
@media only screen and (min-width: 64rem) {
  #cs-footer-144 .cs-container {
    /* pushes all flex children to the top */
    align-items: flex-start;
  }
  #cs-footer-144 .cs-text {
    margin: 0;
  }
  #cs-footer-144 .cs-contact {
    padding: 0;
    margin: 0;
    /* 4px - 12px */
    margin-top: clamp(0.25rem, 0.6vw, 0.75rem);
    width: auto;
    /* reset the flex */
    display: block;
    border: none;
  }
  #cs-footer-144 .cs-contact-li {
    text-align: right;
    margin-bottom: 2.5rem;
  }
  #cs-footer-144 .cs-contact-link {
    text-align: right;
    /* make the second part of the address drop to second line */
  }
  #cs-footer-144 .cs-contact-link span {
    display: block;
  }
  #cs-footer-144 .cs-bottom {
    /* 60px - 80px */
    margin-top: clamp(3.75rem, 5.9vw, 5rem);
    border-top: 1px solid #DAD9E3;
  }
}
                                
/*-- -------------------------- -->
<---          Footer            -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
  :root {
    /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
    --primary: #001F3F;
    --primaryLight: #001F3F;
    --secondary: #FFBA43;
    --secondaryLight: #FFBA43;
    --headerColor: #1a1a1a;
    --bodyTextColor: #4E4B66;
    --bodyTextColorWhite: #FAFBFC;
    /* 13px - 16px */
    --topperFontSize: clamp(0.8125rem, 1.6vw, 1rem);
    /* 31px - 49px */
    --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
    --bodyFontSize: 1rem;
    /* 60px - 100px top and bottom */
    --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
  }
  #cs-footer-144 {
    /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
    font-family: 'Roboto', 'Arial', sans-serif;
    /* 40px - 100px top and bottom */
    padding: clamp(2.5rem, 7.9vw, 6.25rem) 1rem;
    /* Navigation Links */
    /* Contact Links */
    /* Bottom Credits and Social */
  }
  #cs-footer-144 .cs-container {
    width: 100%;
    /* reset on tablet */
    max-width: 34.375rem;
    margin: auto;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
  }
  #cs-footer-144 .cs-logo-group {
    /* takes up all the space, lets the other ul's wrap below it */
    width: 100%;
  }
  #cs-footer-144 .cs-logo {
    /* 210px - 240px */
    width: clamp(13.125rem, 8vw, 15rem);
    height: auto;
    display: block;
    /* 28px - 44px */
    margin-bottom: clamp(1.75rem, 4.17vw, 2.75rem);
  }
  #cs-footer-144 .cs-text {
    /* 14px - 16px */
    font-size: clamp(0.875rem, 1.7vw, 1rem);
    margin: 0;
    margin-bottom: 2rem;
    max-width: 20.5rem;
    color: var(--bodyTextColor);
  }
  #cs-footer-144 .cs-logo-img {
    width: 100%;
    height: auto;
  }
  #cs-footer-144 .cs-nav {
    padding: 0;
    margin: 0;
  }
  #cs-footer-144 .cs-nav-li {
    list-style: none;
    margin-bottom: 1.25rem;
  }
  #cs-footer-144 .cs-nav-li:last-of-type {
    margin-bottom: 0;
  }
  #cs-footer-144 .cs-header {
    /* 18px - 20px */
    font-size: clamp(1.125rem, 1.5vw, 1.25rem);
    line-height: 1.5em;
    font-weight: 700;
    color: var(--bodyTextColor);
  }
  #cs-footer-144 .cs-nav-link {
    font-size: 1rem;
    text-decoration: none;
    line-height: 1.5em;
    color: var(--bodyTextColor);
    position: relative;
  }
  #cs-footer-144 .cs-nav-link:before {
    /* underline */
    content: '';
    width: 0%;
    height: 0.125rem;
    background: var(--bodyTextColor);
    opacity: 1;
    position: absolute;
    display: block;
    bottom: -0.125rem;
    left: 0;
    transition: width .3s;
  }
  #cs-footer-144 .cs-nav-link:hover:before {
    width: 100%;
  }
  #cs-footer-144 .cs-contact {
    margin: 2.25rem 0 0;
    padding: 1.5rem 0 1.75rem;
    width: 100%;
    border-top: 1px solid #DAD9E3;
    border-bottom: 1px solid #DAD9E3;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
  }
  #cs-footer-144 .cs-contact-li {
    list-style: none;
    margin-bottom: 0.75rem;
  }
  #cs-footer-144 .cs-contact-li:last-of-type {
    margin-bottom: 0;
  }
  #cs-footer-144 .cs-contact-link {
    /* 14px - 20px */
    font-size: clamp(0.875rem, 1.57vw, 1.25rem);
    text-decoration: none;
    font-weight: 700;
    line-height: 1.5em;
    text-align: center;
    color: var(--bodyTextColor);
    display: inline-block;
  }
  #cs-footer-144 .cs-contact-link:hover {
    text-decoration: underline;
  }
  #cs-footer-144 .cs-address {
    /* 14px - 16px */
    font-size: clamp(0.875rem, 1.57vw, 1rem);
  }
  #cs-footer-144 .cs-bottom {
    /* 20px - 28px */
    padding-top: clamp(1.25rem, 2vw, 1.75rem);
    width: 100%;
    display: flex;
    /* set everyone to position on the left side */
    justify-content: flex-start;
    align-items: center;
    /* These two are Needed to make 3D transforms look 3D on a child */
    perspective: 700px;
    transform-style: preserve-3d;
  }
  #cs-footer-144 .cs-copyright {
    font-size: 0.9375rem;
    text-align: center;
    color: #7D799C;
    display: block;
    /* margin right auto on a flexbox pushes everyone else to the right */
    margin: 0 auto 0 0;
  }
  #cs-footer-144 .cs-social-link {
    width: 1.25rem;
    height: 1.25rem;
    margin-left: 0.75rem;
    background-color: #4E4B66;
    border-radius: 50%;
    display: flex;
    justify-content: center;
    align-items: center;
    position: relative;
    z-index: 1;
    transition: transform 0.3s, background-color 0.3s;
  }
  #cs-footer-144 .cs-social-link:hover {
    background-color: #1a1a1a;
    transform: translateY(-0.1875rem);
  }
  #cs-footer-144 .cs-social-img {
    height: 0.6875rem;
    width: auto;
    display: block;
  }
}
/* Tablet - 768px */
@media only screen and (min-width: 48rem) {
  #cs-footer-144 .cs-container {
    max-width: 80rem;
  }
  #cs-footer-144 .cs-logo-group {
    width: auto;
    /* 224px - 305px */
    width: clamp(14rem, 23vw, 19.0625rem);
  }
  #cs-footer-144 .cs-nav {
    /* 4px - 12px */
    margin-top: clamp(0.25rem, 0.6vw, 0.75rem);
  }
  #cs-footer-144 .cs-contact {
    flex-direction: row;
    justify-content: space-between;
    border-top: none;
  }
  #cs-footer-144 .cs-contact-li {
    margin: 0;
  }
}
/* Small Desktop - 1024px */
@media only screen and (min-width: 64rem) {
  #cs-footer-144 .cs-container {
    /* pushes all flex children to the top */
    align-items: flex-start;
  }
  #cs-footer-144 .cs-text {
    margin: 0;
  }
  #cs-footer-144 .cs-contact {
    padding: 0;
    margin: 0;
    /* 4px - 12px */
    margin-top: clamp(0.25rem, 0.6vw, 0.75rem);
    width: auto;
    /* reset the flex */
    display: block;
    border: none;
  }
  #cs-footer-144 .cs-contact-li {
    text-align: right;
    margin-bottom: 2.5rem;
  }
  #cs-footer-144 .cs-contact-link {
    text-align: right;
    /* make the second part of the address drop to second line */
  }
  #cs-footer-144 .cs-contact-link span {
    display: block;
  }
  #cs-footer-144 .cs-bottom {
    /* 60px - 80px */
    margin-top: clamp(3.75rem, 5.9vw, 5rem);
    border-top: 1px solid #DAD9E3;
  }
}
/* Dark Mode */
@media only screen and (min-width: 0rem) {
  body.dark-mode #cs-footer-144 .cs-nav-link,
  body.dark-mode #cs-footer-144 .cs-contact-link,
  body.dark-mode #cs-footer-144 .cs-header,
  body.dark-mode #cs-footer-144 .cs-text {
    color: var(--bodyTextColorWhite);
  }
  body.dark-mode #cs-footer-144 .cs-nav-link:before,
  body.dark-mode #cs-footer-144 .cs-contact-link:before,
  body.dark-mode #cs-footer-144 .cs-header:before,
  body.dark-mode #cs-footer-144 .cs-text:before {
    background-color: var(--bodyTextColorWhite);
  }
  body.dark-mode #cs-footer-144 .cs-logo {
    /* turns it white */
    filter: grayscale(1) brightness(1000%);
  }
  body.dark-mode #cs-footer-144 .cs-copyright {
    color: #B4B2C7;
  }
}
                                
/*-- -------------------------- -->
<---          Footer            -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
    :root {
        /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
        --primary: #001F3F;
        --primaryLight: #001F3F;
        --secondary: #FFBA43;
        --secondaryLight: #FFBA43;
        --headerColor: #1a1a1a;
        --bodyTextColor: #4E4B66;
		--bodyTextColorWhite: #FAFBFC;

        /* 13px - 16px */
        --topperFontSize: clamp(.8125rem, 1.6vw, 1rem);
        /* 31px - 49px */
        --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
        --bodyFontSize: 1rem;

        /* 60px - 100px top and bottom */
        --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
    }

    #cs-footer-144 {
        /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
        font-family: 'Roboto', 'Arial', sans-serif;
        /* 40px - 100px top and bottom */
           padding: clamp(2.5rem, 7.9vw, 6.25rem) (16/16rem);
   
        .cs-container {
            width: 100%;
            /* reset on tablet */
            max-width: (550/16rem);
            margin: auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
        }

        .cs-logo-group {
            /* takes up all the space, lets the other ul's wrap below it */
            width: 100%;
        }

        .cs-logo {
            /* 210px - 240px */
            width: clamp(13.125rem, 8vw, 15rem);
            height: auto;
            display: block;
            /* 28px - 44px */
            margin-bottom: clamp(1.75rem, 4.17vw, 2.75rem);
        }

        .cs-text {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.7vw, 1rem);
            margin: 0;
            margin-bottom: (32/16rem);
            max-width: (328/16rem);
            color: var(--bodyTextColor);
        }

        .cs-logo-img {
            width: 100%;
            height: auto;
        }

        /* Navigation Links */
        .cs-nav {
            padding: 0;
            margin: 0;
        }

        .cs-nav-li {
            list-style: none;
            margin-bottom: (20/16rem);
            &:last-of-type {
                margin-bottom: 0;
            }
        }

        .cs-header {
            /* 18px - 20px */
            font-size: clamp(1.125rem, 1.5vw, 1.25rem);
            line-height: 1.5em;
            font-weight: 700;
            color: var(--bodyTextColor);
        }

        .cs-nav-link {
            font-size: (16/16rem);
            text-decoration: none;
            line-height: 1.5em;
            color: var(--bodyTextColor);
            position: relative;

            &:before {
                /* underline */
                content: '';
                width: 0%;
                height: (2/16rem);
                background: var(--bodyTextColor);
                opacity: 1;
                position: absolute;
                display: block;
                bottom: (-2/16rem);
                left: 0;
                transition: width .3s;
            }

            &:hover {
                &:before {
                    width: 100%;
                }
            }
        }

        /* Contact Links */
        .cs-contact {
            margin: (36/16rem) 0 0;
            padding: (24/16rem) 0 (28/16rem);
            width: 100%;
            border-top: 1px solid #DAD9E3;
            border-bottom: 1px solid #DAD9E3;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .cs-contact-li {
            list-style: none;
            margin-bottom: (12/16rem);
            &:last-of-type {
                margin-bottom: 0;
            }
        }

        .cs-contact-link {
            /* 14px - 20px */
            font-size: clamp(.875rem, 1.57vw, 1.25rem);
            text-decoration: none;
            font-weight: 700;
            line-height: 1.5em;
            text-align: center;
            color: var(--bodyTextColor);
            display: inline-block;

            &:hover {
                text-decoration: underline;
            }
        }

        .cs-address {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.57vw, 1rem);
        }

        /* Bottom Credits and Social */
        .cs-bottom {
            /* 20px - 28px */
            padding-top: clamp(1.25rem, 2vw, 1.75rem);
            width: 100%;
            display: flex;
            /* set everyone to position on the left side */
            justify-content: flex-start;
            align-items: center;
            /* These two are Needed to make 3D transforms look 3D on a child */
            perspective: 700px;
            transform-style: preserve-3d;
        }
    
        .cs-copyright {
            font-size: (15/16rem);
            text-align: center;
            color: #7D799C;
            display: block;
            /* margin right auto on a flexbox pushes everyone else to the right */
            margin: 0 auto 0 0;
        }
    
        .cs-social-link {
            width: (20/16rem);
            height: (20/16rem);
            margin-left: (12/16rem);
            background-color: #4E4B66;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
            z-index: 1;
            transition: transform .3s, background-color .3s;

            &:hover {
                background-color: #1a1a1a;
                transform: translateY(-3/16rem);
            }
        }

        .cs-social-img {
            height: (11/16rem);
            width: auto;
            display: block;
        }
    }
}

/* Tablet - 768px */
@media only screen and (min-width: 48rem) {
    #cs-footer-144 {
        .cs-container {
            max-width: (1280/16rem);
        }

        .cs-logo-group {
            width: auto;
            /* 224px - 305px */
            width: clamp(14rem, 23vw, 19.0625rem);
        }

        .cs-nav {
            /* 4px - 12px */
            margin-top: clamp(.25rem, .6vw, .75rem);
        }

        .cs-contact {
            flex-direction: row;
            justify-content: space-between;
            border-top: none;
        }

        .cs-contact-li {
            margin: 0;
        }
    }
}

/* Small Desktop - 1024px */
@media only screen and (min-width: 64rem) {
    #cs-footer-144 {
        .cs-container {
            /* pushes all flex children to the top */
            align-items: flex-start;
        }

        .cs-text {
            margin: 0;
        }

        .cs-contact {
            padding: 0;
            margin: 0;
            /* 4px - 12px */
            margin-top: clamp(.25rem, .6vw, .75rem);
            width: auto;
            /* reset the flex */
            display: block;
            border: none;
        }

        .cs-contact-li {
            text-align: right;
            margin-bottom: (40/16rem);
        }

        .cs-contact-link {
            text-align: right;
            /* make the second part of the address drop to second line */
            span {
                display: block;
            }
        }

        .cs-bottom {
            /* 60px - 80px */
            margin-top: clamp(3.75rem, 5.9vw, 5rem);
            border-top: 1px solid #DAD9E3;
        }
    }
}
                                
/*-- -------------------------- -->
<---          Footer            -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
    :root {
        /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
        --primary: #001F3F;
        --primaryLight: #001F3F;
        --secondary: #FFBA43;
        --secondaryLight: #FFBA43;
        --headerColor: #1a1a1a;
        --bodyTextColor: #4E4B66;
		--bodyTextColorWhite: #FAFBFC;

        /* 13px - 16px */
        --topperFontSize: clamp(.8125rem, 1.6vw, 1rem);
        /* 31px - 49px */
        --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
        --bodyFontSize: 1rem;

        /* 60px - 100px top and bottom */
        --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
    }

    #cs-footer-144 {
        /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
        font-family: 'Roboto', 'Arial', sans-serif;
        /* 40px - 100px top and bottom */
           padding: clamp(2.5rem, 7.9vw, 6.25rem) (16/16rem);
   
        .cs-container {
            width: 100%;
            /* reset on tablet */
            max-width: (550/16rem);
            margin: auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
        }

        .cs-logo-group {
            /* takes up all the space, lets the other ul's wrap below it */
            width: 100%;
        }

        .cs-logo {
            /* 210px - 240px */
            width: clamp(13.125rem, 8vw, 15rem);
            height: auto;
            display: block;
            /* 28px - 44px */
            margin-bottom: clamp(1.75rem, 4.17vw, 2.75rem);
        }

        .cs-text {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.7vw, 1rem);
            margin: 0;
            margin-bottom: (32/16rem);
            max-width: (328/16rem);
            color: var(--bodyTextColor);
        }

        .cs-logo-img {
            width: 100%;
            height: auto;
        }

        /* Navigation Links */
        .cs-nav {
            padding: 0;
            margin: 0;
        }

        .cs-nav-li {
            list-style: none;
            margin-bottom: (20/16rem);
            &:last-of-type {
                margin-bottom: 0;
            }
        }

        .cs-header {
            /* 18px - 20px */
            font-size: clamp(1.125rem, 1.5vw, 1.25rem);
            line-height: 1.5em;
            font-weight: 700;
            color: var(--bodyTextColor);
        }

        .cs-nav-link {
            font-size: (16/16rem);
            text-decoration: none;
            line-height: 1.5em;
            color: var(--bodyTextColor);
            position: relative;

            &:before {
                /* underline */
                content: '';
                width: 0%;
                height: (2/16rem);
                background: var(--bodyTextColor);
                opacity: 1;
                position: absolute;
                display: block;
                bottom: (-2/16rem);
                left: 0;
                transition: width .3s;
            }

            &:hover {
                &:before {
                    width: 100%;
                }
            }
        }

        /* Contact Links */
        .cs-contact {
            margin: (36/16rem) 0 0;
            padding: (24/16rem) 0 (28/16rem);
            width: 100%;
            border-top: 1px solid #DAD9E3;
            border-bottom: 1px solid #DAD9E3;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .cs-contact-li {
            list-style: none;
            margin-bottom: (12/16rem);
            &:last-of-type {
                margin-bottom: 0;
            }
        }

        .cs-contact-link {
            /* 14px - 20px */
            font-size: clamp(.875rem, 1.57vw, 1.25rem);
            text-decoration: none;
            font-weight: 700;
            line-height: 1.5em;
            text-align: center;
            color: var(--bodyTextColor);
            display: inline-block;

            &:hover {
                text-decoration: underline;
            }
        }

        .cs-address {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.57vw, 1rem);
        }

        /* Bottom Credits and Social */
        .cs-bottom {
            /* 20px - 28px */
            padding-top: clamp(1.25rem, 2vw, 1.75rem);
            width: 100%;
            display: flex;
            /* set everyone to position on the left side */
            justify-content: flex-start;
            align-items: center;
            /* These two are Needed to make 3D transforms look 3D on a child */
            perspective: 700px;
            transform-style: preserve-3d;
        }
    
        .cs-copyright {
            font-size: (15/16rem);
            text-align: center;
            color: #7D799C;
            display: block;
            /* margin right auto on a flexbox pushes everyone else to the right */
            margin: 0 auto 0 0;
        }
    
        .cs-social-link {
            width: (20/16rem);
            height: (20/16rem);
            margin-left: (12/16rem);
            background-color: #4E4B66;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
            z-index: 1;
            transition: transform .3s, background-color .3s;

            &:hover {
                background-color: #1a1a1a;
                transform: translateY(-3/16rem);
            }
        }

        .cs-social-img {
            height: (11/16rem);
            width: auto;
            display: block;
        }
    }
}

/* Tablet - 768px */
@media only screen and (min-width: 48rem) {
    #cs-footer-144 {
        .cs-container {
            max-width: (1280/16rem);
        }

        .cs-logo-group {
            width: auto;
            /* 224px - 305px */
            width: clamp(14rem, 23vw, 19.0625rem);
        }

        .cs-nav {
            /* 4px - 12px */
            margin-top: clamp(.25rem, .6vw, .75rem);
        }

        .cs-contact {
            flex-direction: row;
            justify-content: space-between;
            border-top: none;
        }

        .cs-contact-li {
            margin: 0;
        }
    }
}

/* Small Desktop - 1024px */
@media only screen and (min-width: 64rem) {
    #cs-footer-144 {
        .cs-container {
            /* pushes all flex children to the top */
            align-items: flex-start;
        }

        .cs-text {
            margin: 0;
        }

        .cs-contact {
            padding: 0;
            margin: 0;
            /* 4px - 12px */
            margin-top: clamp(.25rem, .6vw, .75rem);
            width: auto;
            /* reset the flex */
            display: block;
            border: none;
        }

        .cs-contact-li {
            text-align: right;
            margin-bottom: (40/16rem);
        }

        .cs-contact-link {
            text-align: right;
            /* make the second part of the address drop to second line */
            span {
                display: block;
            }
        }

        .cs-bottom {
            /* 60px - 80px */
            margin-top: clamp(3.75rem, 5.9vw, 5rem);
            border-top: 1px solid #DAD9E3;
        }
    }
}

/* Dark Mode */
@media only screen and (min-width: 0rem) {
    body.dark-mode {
        #cs-footer-144 {
            .cs-nav-link, .cs-contact-link, .cs-header, .cs-text {
                color: var(--bodyTextColorWhite);

                &:before {
                    background-color: var(--bodyTextColorWhite);
                }
            }

            .cs-logo {
                /* turns it white */
                filter: grayscale(1) brightness(1000%);
            }

            .cs-copyright {
                color: #B4B2C7;
            }
        }
    }
}
                                
/*-- -------------------------- -->
<---          Footer            -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
    :root {
        /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
        --primary: #001F3F;
        --primaryLight: #001F3F;
        --secondary: #FFBA43;
        --secondaryLight: #FFBA43;
        --headerColor: #1a1a1a;
        --bodyTextColor: #4E4B66;
		--bodyTextColorWhite: #FAFBFC;

        /* 13px - 16px */
        --topperFontSize: clamp(.8125rem, 1.6vw, 1rem);
        /* 31px - 49px */
        --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
        --bodyFontSize: 1rem;

        /* 60px - 100px top and bottom */
        --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
    }

    #cs-footer-144 {
        /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
        font-family: 'Roboto', 'Arial', sans-serif;
        /* 40px - 100px top and bottom */
        padding: clamp(2.5rem, 7.9vw, 6.25rem) 16/16rem;
   
        .cs-container {
            width: 100%;
            /* reset on tablet */
            max-width: calc(550 / 16 * 1rem);
            margin: auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
        }

        .cs-logo-group {
            /* takes up all the space, lets the other ul's wrap below it */
            width: 100%;
        }

        .cs-logo {
            /* 210px - 240px */
            width: clamp(13.125rem, 8vw, 15rem);
            height: auto;
            display: block;
            /* 28px - 44px */
            margin-bottom: clamp(1.75rem, 4.17vw, 2.75rem);
        }

        .cs-text {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.7vw, 1rem);
            margin: 0;
            margin-bottom: calc(32 / 16 * 1rem);
            max-width: calc(328 / 16 * 1rem);
            color: var(--bodyTextColor);
        }

        .cs-logo-img {
            width: 100%;
            height: auto;
        }

        /* Navigation Links */
        .cs-nav {
            padding: 0;
            margin: 0;
        }

        .cs-nav-li {
            list-style: none;
            margin-bottom: calc(20 / 16 * 1rem);
            &:last-of-type {
                margin-bottom: 0;
            }
        }

        .cs-header {
            /* 18px - 20px */
            font-size: clamp(1.125rem, 1.5vw, 1.25rem);
            line-height: 1.5em;
            font-weight: 700;
            color: var(--bodyTextColor);
        }

        .cs-nav-link {
            font-size: calc(16 / 16 * 1rem);
            text-decoration: none;
            line-height: 1.5em;
            color: var(--bodyTextColor);
            position: relative;

            &:before {
                /* underline */
                content: '';
                width: 0%;
                height: calc(2 / 16 * 1rem);
                background: var(--bodyTextColor);
                opacity: 1;
                position: absolute;
                display: block;
                bottom: calc(-2 / 16 * 1rem);
                left: 0;
                transition: width .3s;
            }

            &:hover {
                &:before {
                    width: 100%;
                }
            }
        }

        /* Contact Links */
        .cs-contact {
            margin: calc(36 / 16 * 1rem) 0 0;
            padding: calc(24 / 16 * 1rem) 0 calc(28 / 16 * 1rem);
            width: 100%;
            border-top: 1px solid #DAD9E3;
            border-bottom: 1px solid #DAD9E3;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .cs-contact-li {
            list-style: none;
            margin-bottom: calc(12 / 16 * 1rem);
            &:last-of-type {
                margin-bottom: 0;
            }
        }

        .cs-contact-link {
            /* 14px - 20px */
            font-size: clamp(.875rem, 1.57vw, 1.25rem);
            text-decoration: none;
            font-weight: 700;
            line-height: 1.5em;
            text-align: center;
            color: var(--bodyTextColor);
            display: inline-block;

            &:hover {
                text-decoration: underline;
            }
        }

        .cs-address {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.57vw, 1rem);
        }

        /* Bottom Credits and Social */
        .cs-bottom {
            /* 20px - 28px */
            padding-top: clamp(1.25rem, 2vw, 1.75rem);
            width: 100%;
            display: flex;
            /* set everyone to position on the left side */
            justify-content: flex-start;
            align-items: center;
            /* These two are Needed to make 3D transforms look 3D on a child */
            perspective: 700px;
            transform-style: preserve-3d;
        }
    
        .cs-copyright {
            font-size: calc(15 / 16 * 1rem);
            text-align: center;
            color: #7D799C;
            display: block;
            /* margin right auto on a flexbox pushes everyone else to the right */
            margin: 0 auto 0 0;
        }
    
        .cs-social-link {
            width: calc(20 / 16 * 1rem);
            height: calc(20 / 16 * 1rem);
            margin-left: calc(12 / 16 * 1rem);
            background-color: #4E4B66;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
            z-index: 1;
            transition: transform .3s, background-color .3s;

            &:hover {
                background-color: #1a1a1a;
                transform: translateY(-.1875rem);
            }
        }

        .cs-social-img {
            height: calc(11 / 16 * 1rem);
            width: auto;
            display: block;
        }
    }
}

/* Tablet - 768px */
@media only screen and (min-width: 48rem) {
    #cs-footer-144 {
        .cs-container {
            max-width: calc(1280 / 16 * 1rem);
        }

        .cs-logo-group {
            width: auto;
            /* 224px - 305px */
            width: clamp(14rem, 23vw, 19.0625rem);
        }

        .cs-nav {
            /* 4px - 12px */
            margin-top: clamp(.25rem, .6vw, .75rem);
        }

        .cs-contact {
            flex-direction: row;
            justify-content: space-between;
            border-top: none;
        }

        .cs-contact-li {
            margin: 0;
        }
    }
}

/* Small Desktop - 1024px */
@media only screen and (min-width: 64rem) {
    #cs-footer-144 {
        .cs-container {
            /* pushes all flex children to the top */
            align-items: flex-start;
        }

        .cs-text {
            margin: 0;
        }

        .cs-contact {
            padding: 0;
            margin: 0;
            /* 4px - 12px */
            margin-top: clamp(.25rem, .6vw, .75rem);
            width: auto;
            /* reset the flex */
            display: block;
            border: none;
        }

        .cs-contact-li {
            text-align: right;
            margin-bottom: calc(40 / 16 * 1rem);
        }

        .cs-contact-link {
            text-align: right;
            /* make the second part of the address drop to second line */
            span {
                display: block;
            }
        }

        .cs-bottom {
            /* 60px - 80px */
            margin-top: clamp(3.75rem, 5.9vw, 5rem);
            border-top: 1px solid #DAD9E3;
        }
    }
}
                                
/*-- -------------------------- -->
<---          Footer            -->
<--- -------------------------- -*/

/* Mobile - 360px */
@media only screen and (min-width: 0rem) {
    :root {
        /* remove this whole :root and put in your global css file that's shared on all pages, then just remove this section from all other Stitches you add later, only need this once in your global stylesheet */
        --primary: #001F3F;
        --primaryLight: #001F3F;
        --secondary: #FFBA43;
        --secondaryLight: #FFBA43;
        --headerColor: #1a1a1a;
        --bodyTextColor: #4E4B66;
		--bodyTextColorWhite: #FAFBFC;

        /* 13px - 16px */
        --topperFontSize: clamp(.8125rem, 1.6vw, 1rem);
        /* 31px - 49px */
        --headerFontSize: clamp(1.9375rem, 3.9vw, 3.0625rem);
        --bodyFontSize: 1rem;

        /* 60px - 100px top and bottom */
        --sectionPadding: clamp(3.75rem, 7.82vw, 6.25rem) 1rem;
    }

    #cs-footer-144 {
        /* remove the font family so the Stitch inherits the fonts from your global stylesheet */
        font-family: 'Roboto', 'Arial', sans-serif;
        /* 40px - 100px top and bottom */
        padding: clamp(2.5rem, 7.9vw, 6.25rem) 16/16rem;
   
        .cs-container {
            width: 100%;
            /* reset on tablet */
            max-width: calc(550 / 16 * 1rem);
            margin: auto;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
        }

        .cs-logo-group {
            /* takes up all the space, lets the other ul's wrap below it */
            width: 100%;
        }

        .cs-logo {
            /* 210px - 240px */
            width: clamp(13.125rem, 8vw, 15rem);
            height: auto;
            display: block;
            /* 28px - 44px */
            margin-bottom: clamp(1.75rem, 4.17vw, 2.75rem);
        }

        .cs-text {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.7vw, 1rem);
            margin: 0;
            margin-bottom: calc(32 / 16 * 1rem);
            max-width: calc(328 / 16 * 1rem);
            color: var(--bodyTextColor);
        }

        .cs-logo-img {
            width: 100%;
            height: auto;
        }

        /* Navigation Links */
        .cs-nav {
            padding: 0;
            margin: 0;
        }

        .cs-nav-li {
            list-style: none;
            margin-bottom: calc(20 / 16 * 1rem);
            &:last-of-type {
                margin-bottom: 0;
            }
        }

        .cs-header {
            /* 18px - 20px */
            font-size: clamp(1.125rem, 1.5vw, 1.25rem);
            line-height: 1.5em;
            font-weight: 700;
            color: var(--bodyTextColor);
        }

        .cs-nav-link {
            font-size: calc(16 / 16 * 1rem);
            text-decoration: none;
            line-height: 1.5em;
            color: var(--bodyTextColor);
            position: relative;

            &:before {
                /* underline */
                content: '';
                width: 0%;
                height: calc(2 / 16 * 1rem);
                background: var(--bodyTextColor);
                opacity: 1;
                position: absolute;
                display: block;
                bottom: calc(-2 / 16 * 1rem);
                left: 0;
                transition: width .3s;
            }

            &:hover {
                &:before {
                    width: 100%;
                }
            }
        }

        /* Contact Links */
        .cs-contact {
            margin: calc(36 / 16 * 1rem) 0 0;
            padding: calc(24 / 16 * 1rem) 0 calc(28 / 16 * 1rem);
            width: 100%;
            border-top: 1px solid #DAD9E3;
            border-bottom: 1px solid #DAD9E3;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
        }

        .cs-contact-li {
            list-style: none;
            margin-bottom: calc(12 / 16 * 1rem);
            &:last-of-type {
                margin-bottom: 0;
            }
        }

        .cs-contact-link {
            /* 14px - 20px */
            font-size: clamp(.875rem, 1.57vw, 1.25rem);
            text-decoration: none;
            font-weight: 700;
            line-height: 1.5em;
            text-align: center;
            color: var(--bodyTextColor);
            display: inline-block;

            &:hover {
                text-decoration: underline;
            }
        }

        .cs-address {
            /* 14px - 16px */
            font-size: clamp(.875rem, 1.57vw, 1rem);
        }

        /* Bottom Credits and Social */
        .cs-bottom {
            /* 20px - 28px */
            padding-top: clamp(1.25rem, 2vw, 1.75rem);
            width: 100%;
            display: flex;
            /* set everyone to position on the left side */
            justify-content: flex-start;
            align-items: center;
            /* These two are Needed to make 3D transforms look 3D on a child */
            perspective: 700px;
            transform-style: preserve-3d;
        }
    
        .cs-copyright {
            font-size: calc(15 / 16 * 1rem);
            text-align: center;
            color: #7D799C;
            display: block;
            /* margin right auto on a flexbox pushes everyone else to the right */
            margin: 0 auto 0 0;
        }
    
        .cs-social-link {
            width: calc(20 / 16 * 1rem);
            height: calc(20 / 16 * 1rem);
            margin-left: calc(12 / 16 * 1rem);
            background-color: #4E4B66;
            border-radius: 50%;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
            z-index: 1;
            transition: transform .3s, background-color .3s;

            &:hover {
                background-color: #1a1a1a;
                transform: translateY(-.1875rem);
            }
        }

        .cs-social-img {
            height: calc(11 / 16 * 1rem);
            width: auto;
            display: block;
        }
    }
}

/* Tablet - 768px */
@media only screen and (min-width: 48rem) {
    #cs-footer-144 {
        .cs-container {
            max-width: calc(1280 / 16 * 1rem);
        }

        .cs-logo-group {
            width: auto;
            /* 224px - 305px */
            width: clamp(14rem, 23vw, 19.0625rem);
        }

        .cs-nav {
            /* 4px - 12px */
            margin-top: clamp(.25rem, .6vw, .75rem);
        }

        .cs-contact {
            flex-direction: row;
            justify-content: space-between;
            border-top: none;
        }

        .cs-contact-li {
            margin: 0;
        }
    }
}

/* Small Desktop - 1024px */
@media only screen and (min-width: 64rem) {
    #cs-footer-144 {
        .cs-container {
            /* pushes all flex children to the top */
            align-items: flex-start;
        }

        .cs-text {
            margin: 0;
        }

        .cs-contact {
            padding: 0;
            margin: 0;
            /* 4px - 12px */
            margin-top: clamp(.25rem, .6vw, .75rem);
            width: auto;
            /* reset the flex */
            display: block;
            border: none;
        }

        .cs-contact-li {
            text-align: right;
            margin-bottom: calc(40 / 16 * 1rem);
        }

        .cs-contact-link {
            text-align: right;
            /* make the second part of the address drop to second line */
            span {
                display: block;
            }
        }

        .cs-bottom {
            /* 60px - 80px */
            margin-top: clamp(3.75rem, 5.9vw, 5rem);
            border-top: 1px solid #DAD9E3;
        }
    }
}

/* Dark Mode */
@media only screen and (min-width: 0rem) {
    body.dark-mode {
        #cs-footer-144 {
            .cs-nav-link, .cs-contact-link, .cs-header, .cs-text {
                color: var(--bodyTextColorWhite);

                &:before {
                    background-color: var(--bodyTextColorWhite);
                }
            }

            .cs-logo {
                /* turns it white */
                filter: grayscale(1) brightness(1000%);
            }

            .cs-copyright {
                color: #B4B2C7;
            }
        }
    }
}
                                